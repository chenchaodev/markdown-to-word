# 批次 7「体验优化 + 流程简化」验收记录

> 代码 + 自动化验证已完成(2026-08-08 12:16:09);GUI 实测已全部通过(2026-08-09)。
> 实现范围见 `docs/ROADMAP.md` 批次 7 与 `docs/CHANGELOG.md` 0.17.0/0.17.1。

## 待实测清单

### S1 列表增删
- [x] 单文件态「移除」按钮:点击后回到初始态
- [x] 多文件态「追加文件」:对话框追加,重复文件不重复入列(状态区提示跳过)
- [x] 多文件每项「✕」移除按钮:单项移除,剩余序号重排
- [x] 「清空列表」按钮:全部清空回到初始态
- [x] 拖入新文件 = 追加(不替换现有列表)

### S2 输出目录
- [x] 设置面板「输出目录」:显示当前值(默认「源文件所在目录」)
- [x] 「选择…」选目录后转换产物落到该目录;目录不存在时自动创建
- [x] 「恢复默认」回到源文件同目录
- [x] 重名测试:同一文件连续转换两次 → 第二次产物为「名 (2).docx/pdf」,不覆盖

### S3 进度与取消
- [x] 转换中显示进度条 + 百分比 + 取消按钮(合并进度条 bug 524cdf2 已复测通过:合并转 PDF 15%→70%→95% 递进)
- [x] 单文件/批量/合并均可取消;取消后状态区与汇总条显示「已取消」(取消重试 fd40480、PDF 取消 f809c57 均已复测通过)
- [x] 批量取消:未开始项记「已取消,未转换」,成功项产物保留
- [x] PDF 取消说明:printToPDF 为 Electron 原子调用,点取消后需等当前轮打印结束才中止(大文档数秒延迟),最终不产出文件、显示「已取消」

### 失败/警告可见性
- [x] 单文件失败:弹窗标题「转换失败」+ 文件名 + 红色原因(错误三要素)
- [x] 汇总条常驻:完成后显示完成路径 + 「打开所在文件夹 / 打开文件」按钮
- [x] 警告(缺失图片等)折叠在汇总条「警告(N)」内
- [x] 批量失败后「失败详情」按钮重开逐条弹窗

### 编码与非法输入
- [x] GBK 编码的 md 文件可直接转换(状态区/警告提示「已按 GBK 编码读取」)
- [x] 边距输入非法(如 9999)字段内提示并恢复原值
- [x] 字号/行距超范围、字体为空:字段内提示
- [x] 模板微调后下拉回退「默认」并提示「已微调,与模板预设不一致」

### 操作与文案
- [x] Ctrl+Enter 触发主转换(单文件/批量);Ctrl+O 添加文件
- [x] 完成弹窗「复制路径」按钮可复制
- [x] 批量完成后仅首个成功项按「导出后」设置自动打开(不会弹 N 个)

## 实测结果记录
- 2026-08-09:用户 GUI 实测,清单 24 项全部通过(含 3 个修复复测:合并进度条递进、取消后重试正常、PDF 取消不产出文件);批次 7 关闭。

---

## 批次 8「学术正式化延伸」验收记录

> 规划:2026-08-09 开工;已拍板 D1=免更新路线(静态目录 + 静态编号)、D2=前缀行识别(「图: 标题」/「表: 标题」)。
> 范围:8a TOC 开关化 + 免更新静态目录;8b 图/表题注前缀行识别 + 静态编号。8c/8d 排下一迭代。
> 实现说明:题注编号按文档内出现顺序**全文连续**(图 1/2/3、表 1/2 各自独立计数),不挂章节号(规划期「图 1.1」方案在实现时简化为全文连续,与章节自动编号解耦)。

## 待实测清单(自动化断言见 test/segments/toc-caption.test.js)

### 8a 目录
- [x] 默认(开关开):docx 打开即见静态目录(标题列表,可点击跳转,**无页码、无「更新域」提示**);PDF 有目录
- [x] 关闭「自动生成目录」:docx/PDF 均无目录页
- [x] 目录条目不含编号(纯标题文本,与标题自动编号互不干扰)

### 8b 题注
- [x] 含图段落后紧跟「图: 标题」→ docx/PDF 渲染为「图 1 标题」(图/表各自独立全文连续计数)
- [x] 表格后紧跟「表: 标题」→ 「表 1 标题」;图文混排时图/表各自独立计数
- [x] 题注编号与章节自动编号解耦,始终全文连续(图 1、表 1)
- [x] 关闭「题注自动编号」→ 题注行按普通段落渲染(「图: 标题」文本原样保留)
- [x] 边界:孤立「图:」行(前无图/表)按普通段落;题注不进目录/书签

## 实测结果记录
- 2026-08-09:用户实测通过(以 output/artifacts/toc-caption.docx/pdf 为准:目录打开即见无更新域提示、右键更新域可刷新、题注编号与样式正确;PDF 目录与题注编号与正文一致)。批次 8 关闭。

---

## 批次 9「公式编号 + 交叉引用」验收记录

> 规划:2026-08-09 开工(承接原 8d);已拍板 D3=免更新路线延续(静态编号/引用,与 8a/8b 一致)、D4=语法(display 公式自动编号全文连续 + `{#eq:label}` 独立行锚点 + `[式](#eq:label)` 引用)。
> 范围:docx/PDF 双格式 display 公式自动编号 (1)(2)(3)…、label 锚点、交叉引用静态文本「式 (N)」跳转、悬空引用「式 (?)」+ 警告。
> 不做(记后续):题注/章节交叉引用(需 8b 补 label 机制)、公式编号开关。

## 待实测清单(自动化断言见 test/segments/eq-numbering.test.js)

### 公式编号
- [x] docx:display 公式按出现顺序编号 (1)(2)…(行内公式不编号);公式居中、编号右对齐(制表位排版)
- [x] PDF:编号与 docx 一致(全文连续);公式居中、编号右缘垂直居中
- [x] label 锚点行 `{#eq:label}` 不渲染(docx 书签 / PDF 页内锚点)

### 交叉引用
- [x] `[式](#eq:energy)` → docx/PDF 渲染为「式 (1)」可点击跳转到对应公式
- [x] `[公式](#eq:force)` → 「公式 (2)」;悬空引用(未定义 label)→「式 (?)」+ 提示
- [x] 引用编号与公式编号同步(改公式顺序重新导出后一致)

## 实测结果记录
- 2026-08-09:用户实测通过(以 output/artifacts/eq-numbering.docx/pdf 为准:公式渲染、编号右对齐、引用跳转正常)。批次 9 关闭。
- 2026-08-09 修复记录:首轮实测反馈「Word 公式段显示源码文本、PDF 编号未右对齐」→ 修复裸 `<w:tab/>`(包进 w:r)+ 书签 w:id 唯一化(BookmarkStart/End + 自增计数器)+ 验收脚本补 katexDir;复测通过(0.19.1)。

---

## 批次 10「Mermaid 渲染导出」验收记录

> 规划:2026-08-13 开工(功能 1/7,8c);```mermaid 围栏代码块 → docx 嵌入渲染图 + pdf 渲染图表;离线可用(本地 node_modules 加载,无 CDN);渲染失败降级为等宽灰字 + 警告(与公式降级一致)。
> 自动化断言见 test/segments/mermaid.test.js(core 契约 9 断言点)+ test/main/mermaid-service.test.js(真实渲染)。

## 待实测清单(≤5 项)

### M1 双格式渲染
- [x] docx:```mermaid 围栏 → 嵌入渲染图(PNG,尺寸合理不溢出,居中或左对齐与正文一致)
- [x] PDF:```mermaid 围栏 → 渲染图表(浅色主题适配打印,与正文同排版)

### M2 降级与边界
- [x] mermaid 语法错误 → 等宽灰字源码 + 警告(不崩溃,与公式降级一致)
- [x] 非 mermaid 围栏(js/python 等)行为不变(仍走代码块渲染)

### M3 离线与中文
- [x] 离线可用:转换过程无网络请求(本地加载 mermaid.js)
- [x] 中文标签/文本渲染正常(流程图节点中文)

## 实测结果记录
- 2026-08-13:用户 GUI 实测通过(M1 双格式渲染 + M2 降级/边界 + M3 离线/中文全部通过)。批次 10 功能 1 关闭。

---

## 批次 10 功能 2「题注/章节交叉引用」验收记录

> 规划:2026-08-13 开工(原 9d);承接批次 8/9 决策链(D1-D4 免更新路线,静态注入编号 + 超链接跳转);题注(图/表)与章节可被引用,学术正式化闭环。
> 自动化断言见 test/segments/cross-ref.test.js(12 条验收点,2026-08-13 已落地,30 段全绿);人工实测样例:test/fixtures/acceptance/cross-ref.md(GUI 直接拖入)。
> 语法拍板:题注 `图: 标题 {#fig:label}` / `表: 标题 {#tab:label}`;标题 `## 标题 {#sec:label}`;引用 `[图](#fig:label)` / `[表](#tab:label)` / `[章节](#sec:label)`。

## 待实测清单(≤5 项)

### R1 题注引用
- [x] 题注行 label 锚点 → label 不渲染,docx 书签 / PDF 锚点
- [x] `[图](#fig:label)` / `[表](#tab:label)` → 静态「图 N」/「表 N」可点击跳转,编号与题注同步(改顺序重新导出一致)

### R2 章节引用
- [x] 标题行 label 锚点 → docx 书签 / PDF 锚点
- [x] `[章节](#sec:label)` 引用 → 章节号(如「3」「3.2」)+ 跳转;悬空引用 → 「(?)」+ 提示

### R3 兼容与回归
- [x] 现有题注/公式/目录功能不回归;公式交叉引用继续工作

## 实测结果记录
- 2026-08-13:用户 GUI 实测通过(以 test/fixtures/acceptance/cross-ref.md 为准:图/表/章节引用编号与跳转、悬空降级、公式引用不回归;8b 题注序号修复复测正常)。批次 10 功能 2 关闭。

---

## 批次 11「体验打磨」验收记录

> 规划:2026-08-13 开工;方向=体验打磨(个人/技术文档场景),11 项候选全选,拆 4 迭代单元独立提交可回退(e0262e1/dd16075/7d87bed/ebc5d88)。
> 自动化断言:ui-state.test.js(8 场景)/renderer-pure.test.js(batchRetryPaths/batchSuccessPaths/formatRecentTime)/settings.test.js(场景 11 customPresets)/smoke diag 守卫(控件存在性 + 菜单)。

## 待实测清单

### I1 状态记忆(e0262e1)
- [x] 最近转换区块:转换后出现,点击条目一键重转(沿用条目格式),「清空最近」生效
- [x] 重启应用:恢复上次文件列表(缺失文件剔除)、设置面板展开态、窗口位置
- [x] 文件对话框默认打开上次目录;输出目录选择后记忆

### I2 结果增强(dd16075)
- [x] 批量失败:失败详情弹窗「重试失败项」→ 失败项替换列表立即重转(取消项不重试)
- [x] 批量弹窗「复制全部路径」复制成功项路径;完成弹窗「不再提示」+ 设置面板「转换完成弹窗提示」双向同步

### I3 预览与模板(7d87bed)
- [x] 预览窗口:改设置即时刷新;改源文件后 focus 刷新;删文件显示错误页
- [x] 模板:另存为预设(空名/同名拒绝)→ 下拉可选可套用;删除预设回退默认;微调回退「默认」提示

### I4 顺手项(ebc5d88)
- [x] 多文件列表行双击预览(按钮上不触发);Alt 菜单:文件→打开文件…/退出,帮助→关于(版本号)

## 实测结果记录
- 2026-08-14:用户 GUI 实测通过(I1-I4 全项:最近文件一键重转/重启恢复/目录记忆、批量失败重试/复制路径/不再提示、预览即时刷新/模板另存为预设、双击预览/菜单关于)。批次 11 验收关闭,发版 0.25.0。

---

## 批次 12「界面体验优化」验收记录

> 规划:2026-08-14 开工(方向 A,方案存档 docs/archive/20260814-185113-双方向探索方案.md);@designer 评审 20 项问题(P1-P20)+ 12 项候选(C1-C12);本批实施 **Phase 0 速赢 7 项** 拆 4 验收单元。
> 语义决策:多文件态点击拖放区=追加(与文案一致);单文件态点击=更换文件(文案同步);「清空列表」按钮兜底重新选择。
> 自动化:typecheck/build/31 段/smoke 全绿;renderer 交互按「维持人工不自动化」走 GUI 实测。

## 待实测清单(≤5 项)

### U1 点击行为与文案一致(C1)
- [x] 多文件态点击拖放区(含头部/空白区)= 追加文件,不再替换整个列表;误触不丢选择
- [x] 单文件态点击 = 更换文件,文案明确「点击更换文件,或拖入添加」

### U2 布局密度与视口(C3 + C7)
- [x] 窗口有最小尺寸(不可缩到布局挤压);大窗口下内容有宽度上限居中,不拉满;拖放区高度有上限
- [x] 单文件态拖放区压缩(不再 240px+ 大片空白)

### U3 快捷键与文案(C4 + C11)
- [x] 底部可见提示显示快捷键(「Ctrl+Enter 转换 · Ctrl+O 添加文件」,多文件态提示批量语义)
- [x] 标点统一全角、「设置即时生效并自动保存」仅一条、「源文件所在目录」直白化

### U4 反馈修复(C5 + C6)
- [x] 状态区长错误完整可读(悬浮/其他途径查看全文)
- [x] 自定义预设达上限:弹窗内明确提示「已达上限,请先删除」,不再静默丢弃

### U5 单文件态追加按钮(用户反馈)
- [x] 单文件态显示「追加文件」按钮:点击追加合并(去重/非 md 跳过提示),追加后自动切多文件态;转换中禁用
- [x] 有文件时拖入新文件 = 追加(已核实 resolveDropped 无条件追加,非替换)

### U6 模板预设全局常显(C8)
- [x] 模板预设行位于两个设置面板之上常显(不折叠);套用/另存为/删除/微调提示正常

### U7 最近条目仅加载(C12)
- [x] 最近条目「仅加载」按钮:载入列表不转换(可改设置后再转);点击条目主区域仍一键重转

## 实测结果记录
- 2026-08-14 用户 GUI 实测通过(U1-U7 全勾;Phase 1 项 C2 sticky 底部常驻/C10 双击预览提示/C9 弹窗焦点陷阱一并验证通过)

## 批次 13「模板导入(预设 JSON 导入/导出)」验收记录

> 规划:2026-08-14 开工(方案存档 docs/archive/20260814-201622-模板导入方案.md);lib-1 调研五方案对比,拍板首选「预设 JSON 导入/导出」(复用 sanitize 校验,零新依赖);CSS 覆盖 pdf 次选、docx 模板导入暂缓记 ROADMAP。
> 自动化:typecheck/lint/build/32 段/smoke 全绿;导入合并语义走自动化断言,文件对话框走 GUI 实测。

## 待实测清单(≤5 项)

### T1 导出预设
- [x] 模板预设行「导出预设…」:导出全部自定义预设为 JSON 文件(保存对话框);无自定义预设时提示
- [x] 导出文件格式正确(schemaVersion:1 + presets 数组),可被导入回用

### T2 导入预设
- [x] 「导入预设…」:选 JSON 文件 → 追加合并(同名覆盖),上限 10 截断;非法条目丢弃并提示
- [x] 非法文件(非 JSON/缺字段/损坏)明确报错,不破坏现有预设

### T3 导入后应用
- [x] 导入的预设出现在模板下拉(自定义区),可套用/删除;与另存为预设共存

## 实测结果记录
- 2026-08-14 用户 GUI 实测通过(T1-T3 全勾;导出文件可回导、导入合并/覆盖/上限、非法文件报错、导入预设可套用删除均验证)
- 2026-08-14 实测发现并修复「预设下拉弹回」(自定义预设值与硬编码相同时选中被弹回,fix f6e3304),用户复测通过

## 批次 14「测试补齐」验收记录

> 规划:2026-08-15 开工(审计依据 docs/archive/20260815-144057-代码测试文档审计.md);G1-G8 缺口按优先级分立即批(G5/G8/G4)+ 近期批(G1/G2/G3)+ main 错误路径(G6/G7);G9 核实不补。
> **本节为纯自动化项,无 GUI 实测项**(违反入册规则的批次,自 2026-08-24 整形起仅留指针):断言明细见 test/segments/utils.test.js、pdf-postprocess、math、postprocess、bookmarks、converter、mermaid-service 各段与提交 43452ca/9dd272e。

## 实测结果记录
- 2026-08-15 自动化验收通过:typecheck/lint/build/35 段(新增 utils/pdf-postprocess)/smoke 全绿;纯测试补齐不改源码,无 GUI 实测项(9dd272e)

## 公式编号开关(2026-08-16)

> 代码 + 自动化验证已完成(66681a9,36 段全绿);待 GUI 实测。自动断言见 test/segments/eq-numbering.test.js 与 settings 段(开关关闭时 {#eq:label} 隐藏/引用保持原文本),本节只列 GUI 实测项。

- [x] 设置面板「公式编号」默认勾选: 公式正常编号 (1)(2)…、{#eq:label} 行不显示、[式](#eq:label) 显示「式 (N)」+ 跳转
- [x] 取消勾选后转换(docx + pdf):公式不编号、{#eq:label} 行不显示(语法标记隐藏)、引用保持「式」原文本(不降级不警告)
- [x] 开关状态重启应用后保持(持久化)
- [x] 「清空最近」按钮:点击后最近列表清空并隐藏区块(2026-08-16 修复 f9b7d09;批次 11 I1 用户实测「清空最近生效」覆盖复测)
- [x] 设置面板「转换完成弹窗提示」:勾选 = 转换完成弹窗提示、不勾选 = 不提示(2026-08-16 修复 308ebc4,已复测通过)
- [x] 弹窗内「不再提示」勾选后:后续转换不再弹窗;两处 checkbox 联动一致(设置面板勾选状态同步反转)

## 模板导入·CSS 覆盖 pdf 路线(2026-08-16)

> 代码 + 自动化验证完成后待 GUI 实测。

- [x] 设置面板「导入 CSS…」选择 .css 文件 → 状态显示已导入;转换 pdf 后样式生效(如正文颜色/字体被用户 CSS 覆盖)
- [x] 「清除 CSS」后转换 → 恢复默认样式
- [x] 重启应用 CSS 保持(持久化)
- [x] 导入非 .css 文件/读取失败 → 提示错误,不破坏现有设置

## 界面配置区重构(2026-08-16)

> 代码 + 自动化验证完成后待 GUI 实测。

- [x] 「设置」面板折叠时只占 1 行;展开后 4 个子组(模板/页面/排版/导出)组头清晰、分隔线明确
- [x] 模板选择/另存为/删除/导入导出预设/导入清除 CSS 在「模板」组内全部正常
- [x] 最近转换多条时限高 + 滚动条,不撑高界面
- [x] 导出后/弹窗提示/输出目录在「导出」组;公式编号在「排版」组(编号类聚合)
- [x] 标题区无副标题;「设置即时生效并自动保存」提示保留

## 批注(docx 批注支持,2026-08-16)

> 代码 + 自动化验证完成后待 GUI 实测。

- [x] md 写 [锚定文本]{批注=内容} → docx 转换 → Word 打开显示批注(锚定文本 + 批注内容)
- [x] 批注内容支持加粗/斜体等行内 rich
- [x] 多个批注 id 不冲突(Word 正常显示全部批注)
- [x] pdf 转换原样显示批注语法(仅 docx 生效,文档说明)

## WPS 兼容矩阵(2026-08-16)

> 矩阵文档 docs/WPS-COMPAT.md 已建立(功能清单 + 实测指引 + 状态列),待 Word/WPS 双实测。

- [x] 矩阵文档建立(docs/WPS-COMPAT.md:功能清单 + 实测指引 + 状态列 + 问题记录)
- [x] Word 实测:基础排版/学术功能/页面样式核心项通过(矩阵 Word 列更新)
- [x] WPS 实测:基础排版/学术功能/页面样式核心项通过(矩阵 WPS 列更新)
- [x] 差异/问题项记录(矩阵问题记录节 + 状态列;修复排后续迭代)

## 代码块语法高亮写 docx(排期功能 1/3,2026-08-16)

> 代码 + 自动化验证完成后待 GUI 实测。实现范围见 ROADMAP「当前待办·功能候选」。

- [x] docx 产物中带语言代码块逐 token 着色(关键字/字符串/注释等,颜色与 pdf 侧 GitHub Light 一致)
- [x] 无语言/未知语言代码块保持原样(等宽,无着色)
- [x] 代码块行结构保留(逐行 w:br,行不合并)
- [x] 高亮后文本内容完整(含 < > & 等特殊字符正确还原,无丢失/乱码)
- [x] pdf 侧高亮行为不变(回归)

## 实测结果记录
- 2026-08-16:用户 GUI 实测通过(拖入带 ```ts 代码块样例转 docx,着色/降级/行结构/特殊字符均正常);排期功能 1/3 关闭。

## i18n 界面多语言(排期功能 2/3,2026-08-16)

> 代码 + 自动化验证完成后待 GUI 实测。实现范围见 ROADMAP「当前待办·功能候选」。

- [x] 设置面板「界面语言」中文/English 切换,界面文案即时切换(菜单/按钮/弹窗/状态栏/最近文件时间)
- [x] 默认中文:启动后界面全中文,与改造前一致
- [x] 英文界面:主要界面文案均为英文,无中文残留(转换警告除外,设计如此)
- [x] 语言记忆:切换后重启应用保持所选语言
- [x] 回归:切换语言后转换/预览/批量/合并/模板预设均正常

## 实测结果记录
- 2026-08-16:用户 GUI 实测通过(界面验证无问题);排期功能 2/3 关闭。

---

## 批次「B9 UX 体验批」验收记录(2026-08-23)

> 代码 + 自动化验证已完成(45 段/smoke 全绿,提交 8780c14 视觉批+46c0d4d 交互逻辑批);自动断言见 test/segments/renderer-pure.test.js(stageText/actionableError/partitionDuplicates/selectionStatus)与 test/main/ui-state.test.js(isMaximized)。GUI 实测已通过(2026-08-24 用户确认)。

### 进度与错误反馈
- [x] PDF 转换进度分阶段递进(读取→解析→渲染各阶段百分比单调上升,不回退)
- [x] 大文档 PDF 转换末段:取消按钮置灰 + 状态区显示「正在写入 PDF…」
- [x] 转换失败时提示含可操作建议(如文件被占用提示关闭占用程序;文件不存在/无权限/磁盘满各有区分文案)

### 拖放与列表交互
- [x] 转换进行中拖入新文件:状态区出现「转换中请稍后添加」类提示(不静默)
- [x] 拖入含重复文件:重复数单独计数提示;跳过项可展开查看具体文件名
- [x] 最近条目单击=加载到文件列表(不转换);双击=直接重转

### 窗口与输入
- [x] 最大化状态下关闭应用,重开后窗口恢复最大化
- [x] 边距输入框手输超上限值(如 9999)被拦截并字段内提示
- [x] 系统开启「减少动态效果」时弹窗瞬时出现无动画;关闭该选项恢复淡入
- [x] 窗口缩窄至约一半宽度:设置面板降为单列布局,控件不挤压重叠

## 批次「B13 暗色模式」验收记录(2026-08-23)

> 代码 + 自动化验证已完成(45 段/smoke 全绿,提交 5a91a4a);自动断言见 test/segments/settings-logic.test.js(applyThemeOn 直测)。GUI 实测已通过(2026-08-24 用户确认)。

- [x] 设置面板「外观主题」三选一切换即时生效:浅色/深色界面整体变色(背景/文字/按钮/弹窗/列表悬停)
- [x] 「跟随系统」下修改系统深浅色设置,应用界面跟随变化
- [x] 主题选择重启后记忆保持
- [x] 浅色模式(默认)与 1.1.0 版视觉一致(变量收敛未重设计)
- [x] 回归:暗色下拖放区/弹窗/汇总条警告区/进度条均清晰可辨

## 批次「目录结构重组」验收记录(2026-08-23)

> 批①②为 core 域纯移动(GUI 无感,自动断言覆盖);批③④⑤有 GUI 面。代码 + 自动化验证已完成(每批独立提交,45 段/smoke 全绿)。GUI 回归实测已通过(2026-08-24 用户确认;批⑥ main 归组补遗在其后为纯移动 GUI 无感)。

- [x] 全功能回归冒烟:拖放选择/单文件转换(docx+pdf)/批量/合并/预览/取消均正常
- [x] 设置面板全部控件可用且保存生效(模板预设/页面/排版/导出/语言/外观主题)
- [x] 弹窗族正常(完成/批量结果/预设另存/关于),样式与拆分前一致
- [x] 暗色模式下界面与重组前一致(style 四文件拆分未改视觉)

## 实测结果记录
- 2026-08-24:用户 GUI 实测通过(B9 十项/B13 五项/重组回归四项全勾);目录结构重组全部批次关闭。

## 审计整改 + i18n 字典拆分批次(2026-08-24)

> 全库审计整改 P0~P5(61 项中约 54 项实施,裁决见 docs/archive/2026-08-24-193838-审计整改裁决与不做项.md)+ i18n 字典拆分(方案A 分文件+注册表,ja/ko/fr/ru 渐进翻译)。代码+自动化验证已完成(51 段/smoke 全绿;恒等守护见 test/segments/identity-guards.test.js)。以下为人工 GUI 实测项。

- [x] 预览窗尺寸记忆:调整预览窗大小位置后关闭重开,恢复上次尺寸;深色模式下预览错误页配色正常
- [x] shell 白名单:转换后在结果区「打开所在文件夹 / 打开文件」正常;重启应用后点击旧产物路径被拒绝并有明确提示
- [x] 新语言切换:设置面板切换新增语言(日/韩/法/俄),界面即时切换、无裸键名(缺失文案回退英文);重启后记忆保持
- [x] 回归:暗色模式下拖放区/弹窗/汇总条与整改前一致;单文件/批量/合并主链路不回归

## 实测结果记录
- 2026-08-24:用户 GUI 实测通过(4 项全勾);审计整改 + i18n 字典拆分批次关闭,发版 1.3.0。

## 界面改进批次一「布局稳定性」验收记录(2026-08-24)

> 三批界面改进方案的第一批(P0-1 主舞台固定高度 + P0-2 消息槽固定化),根治导入文件后布局跳动。代码+自动化验证已完成(typecheck/build/smoke 全绿;renderer diag 断言 status/dropSkipped 在消息槽内工作正常)。零 TS 逻辑改动(仅 file-list.ts 注释修正),i18n 零新键。以下为人工 GUI 实测项。

### 布局稳定性(核心)
- [ ] 导入不跳动:空态→拖入 1 个文件→追加至多个→逐个移除回空态,全程拖放区外框高度恒定;状态行/最近转换/设置面板/底部转换按钮均不上下位移
- [ ] 转换反馈不推挤:执行一次转换,汇总条出现在消息槽且页面其余部分不动;展开「警告(N)」折叠条时 footer 不被推出视口
- [ ] 设置面板展开:面板体自身内部滚动(约 42vh 上限),footer 转换按钮/进度条保持可见;收起后布局复原

### 单文件紧凑卡与列表
- [ ] 单文件态:图标+文件名+路径+「追加/预览/移除」一行紧凑卡;长文件名/路径省略号截断;点卡片空白弹选文件对话框、行内按钮不误触
- [ ] 多文件:10+ 文件时列表在主舞台内部滚动,序号/上移/下移/预览/移除/拖拽排序可用,主舞台高度不变

### 回归
- [ ] 最近转换区:单击加载/双击重转/仅加载/清空行为不变;≥2 文件隐藏时不引起跳动
- [ ] 窗口边界:默认 900×640 与最小 720×560 下无滚动条无裁切;最大化下主舞台封顶、留白自然
- [ ] 深色模式三态回归:消息槽空态留白/单文件卡/汇总条语义色正常
- [ ] 版本号显示在标题右侧同行(此前折行缺陷已顺带修复)

## 实测结果记录
- (待用户 GUI 实测)
