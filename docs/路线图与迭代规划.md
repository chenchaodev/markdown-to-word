# 路线图与迭代规划

> 2026-08-02 制定,同日因需求变更(GUI)修订。输入:库调研(见 docs/研究结论.md)+ @oracle 架构评审(ADR-001/002,见 docs/架构决策.md)。2026-08-02 晚二期规划经 @oracle 评审重排(评审结论见 docs/研究结论.md)。

## 进展状态
- 2026-08-08:二期四批全部收官;三路调研完成(GUI 操作流程勘察 exp-1 / 功能扩展 lib-1 / 易用性最佳实践 lib-2,结论落盘 docs/研究结论.md + 原文存档 2026-08-08-1029/1030/1031);用户选定**先体验优化批次 7,后功能扩展批次 8**
- 2026-08-06:批次 6「学术正式化」完成(预设模板包 3a0dc8d + 公式双格式 ea14bd0;公式 PDF KaTeX / docx OMML,13 断言全过);批次 6 两项待用户 GUI 实测
- 2026-08-04:二期批次 3「批量 + 合并」完成且用户实测通过(拖放取路径修复 + 文件排序 + 批量/合并转换);实测反馈:PDF 侧边栏书签为空(页面内目录正常)→ 批次 4 开工,**书签优先**
- 2026-08-03:二期批次 1/2 完成且用户验收通过;后续规划经 @oracle 评审 + @librarian 调研重排(批次 3 拆批,新增批次 4/5/6,见下;评审见 docs/研究结论.md / 原文存档 2026-08-03-2325/2326)
- 下一步:批次 4「长文档」(PDF 书签优先)
- 2026-08-02:G5 完成,全部里程碑达成(安装包可装可跑);应用 icon 完成

## 需求范围
- Windows 桌面 GUI 应用:选择 markdown 文件 → 转 .docx / .pdf
- GUI 界面中文
- 中文内容排版可控(硬需求)
- 架构预留其他格式扩展(「等」)

## 功能规划
### MVP
- 文件选择:对话框 + 拖放
- 格式选择:docx / pdf
- 转换执行 + 进度反馈(解析 → 渲染 → 打印 三段)
- 输出位置:默认同目录同名换扩展名,可选另存

### 一期不做
- 预览(HTML 模板已铺路,二期低成本)
- 批量转换
- 设置面板(字体走 core options 默认)
- 自动更新与签名

## 二期规划(2026-08-02 @oracle 评审后定稿;评审结论见 docs/研究结论.md)
按三批独立交付,每批含验收标准。优先级调整要点:页面设置 P1→P0(排版护城河)、PDF 书签 P1→P2(最硬项先 spike)、代码高亮主题切换延后、Mermaid/CLI 砍。

### 批次 1「排版控制 + 设置底座」
设置持久化 + 记忆上次选项(手写 userData JSON,不引 electron-store)· 导出后行为(打开所在文件夹/打开文件)· 分页控制(H1 前分页 + 显式分页符)· 页面设置面板(纸张/边距/方向)· 标题 slug/id 生成(公共底座)
- 价值:护城河第一波(中文排版可控完整落地)+ 后续全部功能的设置底座
- 验收:设置重启后持久;docx/pdf 双格式下分页符、纸张、边距、方向实测正确(中文文档目测);显式分页符语法生效;打开所在文件夹/打开文件生效

### 批次 2「保真 + 正式文档化」
外链图片下载嵌入 · 目录 TOC(docx Word 域 spike 先行 / PDF 无页码锚点目录)· 封面页 + YAML 元数据(title/author/date)· PDF 预览(顺带)
- 价值:行业 #1 投诉(图片丢失)闭环 + 正式文档标配(TOC/封面/标题),「真 docx」卖点坐实
- 验收:外链图断网可用 + 失败警告文案正确;docx TOC 在 Word 与 WPS 双实测 F9 更新;PDF 目录点击跳转(先实测 printToPDF 保留内部锚点链接);YAML 封面/标题生效;预览显示正确排版

### 批次 3「批量 + 合并」(2026-08-03 @oracle 评审后拆分)✅ 完成
批量转换(多选/拖放文件夹 + 队列 + 失败逐条汇总)· 多文件合并(渲染前 md 拼接:文件序 + `<!-- page-break -->`,frontmatter 仅取首个,封面/全局 TOC 自动成立)
- 价值:呼声最高的需求(#1)+ 合并近零成本
- 验收:10 文件批量转换 + 失败汇总报告逐条正确;合并后图片路径不破、目录/层级正确
- 风险(低):pdf 并发限 2(每文件一隐藏窗口);批量共享 imageResolver(现每次新建,去重缓存跨文件失效);合并前逐文件把相对图片转绝对(各文件 baseDir 不同)
- 实测补充:拖放取路径改用 `webUtils.getPathForFile`(File.path 已被 Electron 32+ 移除);文件列表支持排序(拖拽 + 上移/下移,重排 selectedFiles 影响批量/合并顺序)

### 批次 4「长文档」(原批次 3 长文档部分)🔄 开工(书签优先)
PDF 书签(改方向:读 printToPDF 产出的 /Dests 命名目标 → pdf-lib 注入大纲,免 pdfjs 文本定位,风险高→中低;注意 /Dests key 编码,中文 slug 为 UTF-16BE hex)· 脚注(docx FootNotes + markdown-it-footnote 验 14 兼容)· 页眉页脚 + 页码(docx Header/Footer/PageNumber;PDF 模板字符串)
- 价值:长文档正式性;页眉页脚页码为竞品最痛缺口(mdclaudy 五问题之首)
- 验收:书签在 Acrobat/浏览器跳转正确(红线=H1/H2 最小版或砍);脚注 Word/WPS 双实测;页眉页脚/页码双格式实测
- 触发:批次 3 用户实测反馈「PDF 侧边栏书签为空,页面内目录正常」——printToPDF 不产出书签大纲,需 pdf-lib 后处理注入(已实测产物含 /Dests 命名目标 + 18 个 Link 注释,锚点链接存在但无大纲)

### 批次 5「中文排版深化 + 保真补全」
字体设置(ascii/eastAsia 分离,theme.ts 底座已有)· 字号/行距/首行缩进 2 字符/两端对齐(中文刚需)· 章节自动编号(放开标题深度)· docx 内部链接跳转(Bookmark + slug 底座就绪)· raw HTML 白名单(顺手)· PDF 元数据 from frontmatter(title/author/date,顺手)
- 价值:正面对抗竞品最弱项(Pandoc 修 eastAsia 4 年);护城河重心从「格式正确」转向「排版可定制」
- 验收:设置面板 + 双格式中文产物对照;smoke 扩展;编号与 TOC 协同
- 风险(中):两路线参数漂移 → 每项设置双格式实测

### 批次 6「学术正式化」✅ 完成
公式(KaTeX:PDF 侧 woff2 本地嵌入;docx OMML 仅 spike)· 预设模板包(排版设置完成后的参数化)
- 价值:技术文档/论文用户「可编辑公式」硬需求(2026 新竞品 tex2word 以 OMML+自动编号域为卖点)
- 验收:公式双格式实测;模板套用生效
- 红线:OMML 不承诺交付,子集 + 降级纯文本/源码;KaTeX 字体嵌入中
- 落地(2026-08-06):模板包 3a0dc8d;公式 ea14bd0(PDF KaTeX + docx KaTeX MathML→docx Math 组件,OMML 实际超额交付);待用户 GUI 实测

### 批次 7「体验优化 + 流程简化」(2026-08-08 规划,来源:exp-1 勘察 S1-S15 + lib-2 易用性 R1-R18)
- **流程**:① 多文件列表**增删单项**(S1,当前只能整体重选);② 输出位置**可见可选**(S2+R3:设置加输出目录,默认源目录);③ 完成**汇总 + 打开引导**常驻按钮(lib-2 Top1);④ 批量**免保存对话框**(R2,现状已同目录直出,确认对齐)
- **反馈**:⑤ 进度条 + 取消(S3+R5/R7,单文件也量化);⑥ 失败/警告可见性(S4/S5:失败弹窗一致、警告不覆盖完成路径);⑦ 错误三要素文案 + 失败行内重试(R9/R9b)
- **防坑**:⑧ 重名自动加序号绝不覆盖(lib-2 Top2);⑨ 编码预检 GBK/GB18030 自动转码 + 路径超长预检(R10);⑩ 非法输入/模板回退提示(S6/S9);⑪ 导出后行为批量一致(S7)
- **收尾**:⑫ 快捷键(Ctrl+Enter/Ctrl+O/Del,R8);⑬ 复制路径按钮(S11);⑭ 设置写盘失败提示(S8);⑮ 文案统一(用户语言,R16-18)
- 价值:直接命中「文件去哪了 / 转换多久 / 失败怎么办」三大新手痛点;低成本高感知
- 验收:代码 + 验收脚本断言(重名序号、编码、增删、汇总);GUI 实测清单

### 批次 8「功能扩展:学术正式化延伸」(2026-08-08 规划,来源:lib-1 竞品调研;勘察 exp-1 + 域 API 调研 lib-1/lib-3 已完成,落盘 docs/研究结论.md)
按价值/成本排序,起手 3-4 项:

**8a Word 原生 TOC 域开关化(低,勘察后范围缩小)**
- 现状:**原生 TOC 域已存在**——render.ts:289-309 用 docx 9.x `TableOfContents` 组件(`\o "1-3" \h \z \u` 配置齐全,占位「右键 → 更新域 生成」),正文含任意标题即**无条件插入**(render.ts:184-187,无开关)
- 增量:① 开关化 `toc?: boolean`(默认开,仿 breakBeforeH1 顶层布尔;PDF 侧 buildTocHtml 同开关,规则对齐);② `features.updateFields` 联动(官方文档明确要求:Word 打开弹提示并全量更新域);③ 题注不污染目录(题注用独立 Caption 段落样式,不挂标题样式即不被 `\o "1-3"` 收集)
- 红线(维持):静态标题列表 + F9 提示兜底
- 验收:make-batch4-sample.mjs 第 9 段(document.xml 匹配 `w:instrText` TOC 指令 / w:sdt);Word+WPS 双实测 F9

**8b 图/表题注自动编号(中,中文「图1.1」差异化最强单点,Typora 生态做不了)**
- 现状:mdast **无 caption 节点**(grep caption/figcaption 零命中),需自定义识别语法;标题编号已是 **w:numPr numbering 静态渲染**(render.ts:425-428,免 F9)→ STYLEREF 域可直接取章节号(**styleId 写 Heading1**,非 Word 内置 `1`)
- **决策点 D1(先定,8a/8b 必须同路线,严禁混用)**:
  - **更新域路线**(推荐,Word 继续编辑场景):8a `updateFields:true` + 8b `SimpleField(" STYLEREF Heading1 \\s ")` + `SequentialIdentifier("图")`(=SEQ 域);Word 打开弹一次提示全量更新,改标题/增删图后 F9 同步,与 Word 原生题注同构;WPS 兼容未官方背书需实测(12.8 起 TOC 引号 bug 已修;F9 弹安全声明)
  - **免更新路线**(一次性定稿导出场景):8a `beginDirty:false` + cachedEntries 纯超链接(免页码)+ 8b 渲染期静态注入「图 1.1」文本;零提示全端一致,改标题后需重新导出
- **决策点 D2**:题注语法(候选:图/表段落后紧跟「图: 标题」前缀行识别;或 Typora 式 alt 提取——alt 已被占位文本复用,倾向前缀行识别)
- docx 实现:渲染期预扫(仿脚注预扫 render.ts:174-178)建文档级题注上下文{类型,序号,章节号};插入点 renderBlock "paragraph"/"table" case(render.ts:352-360)产出后追加题注段落
- PDF 实现:标题编号已 CSS counter 伪元素(pdf/render.ts:369-377),题注 `.fig-caption::before { content: counter(h1c) "." counter(figc) }` 同机制对齐
- 验收:docx document.xml 匹配 SEQ/STYLEREF 指令(或静态题注文本);PDF html 匹配 fig-caption;Word+WPS 双实测 F9

**8c Mermaid 渲染导出(中,排 8a/8b 后)**:PDF 路线近白送(Chromium 有 DOM,可隐藏窗口渲染),docx 嵌入 SVG/PNG——原「砍」列表理由「无 DOM 环境」在 Electron 内不成立,重新评估升回

**8d 公式编号与交叉引用域(中,排 8a/8b 后)**:已有 KaTeX→OMML;对齐 tex2word 卖点;REF 域用库内置 `NumberedItemReference`(书签+`\h \w`)或 `PageReference`,Bookmark 类确认可用(docx 9.x 对象字面量构造)

- 备选:代码块语法高亮写 docx(低中)、模板导入 MVP(中高,先导出侧)、批注(低)、WPS 兼容矩阵(低,守护既有功能)
- 暂缓:完整 CSL 参考文献、AI 改写、表格合并单元格、文档加密
- 调研证据:TableOfContents/SimpleField/SequentialIdentifier/NumberedItemReference API、STYLEREF 章节号、updateFields 行为、WPS 兼容坑,详见 docs/2026-08-08-1142-docx域API调研.md

### 延后(不排批)
- raw HTML 白名单:随时可做(安全合规),无批次依赖(已排批次 5)
- docx OMML 公式:只承诺 spike 不承诺交付,预设「子集支持 + 降级纯文本/源码」兜底(已排批次 6)
- 代码高亮主题切换:PDF 模板硬编码 GitHub Light,打印场景需求趋零;若做仅 2-3 主题挂设置;docx 代码高亮(逐 token 着色,中高成本)延后,PDF 侧可先做
- 最近文件:低价值低成本,尾部便利
- 模板导入系统(用户上传 docx/CSS):中成本,排在样式模板后
- 砍:Mermaid(无 DOM 环境,成本最高价值最低)、CLI 转正(已有 cli/index.ts 调试通道)、自动更新/签名(本地离线隐私卖点,更新反噬)、i18n、目录监视/同步、PDF 多栏(无竞品证据)、批量重命名(无需求信号)、pagedjs(竞品 TODO 未落地)

### 已定技术取舍(2026-08-02 @oracle 评审)
- 显式分页符语法:`<!-- page-break -->`(remark 产 html 节点,不占用 `---` 的 hr 语义,兼容 Typora 生态)
- 外链图片下载失败策略:保留原 URL 引用 + 警告(保真优先,与现有缺失图片警告同构)
- PDF 目录:无页码 + 锚点链接先行;开工先实测 printToPDF 是否保留页内锚点为 PDF 链接注释(若不保留,PDF 目录降级为纯文本列表)
- docx TOC:docx 9.x SimpleField/ComplexField 拼 `TOC \o "1-3" \h \z \u`,开工即 spike(半天);验收 Word+WPS 双实测 F9;红线 = 静态标题列表 + F9 提示 + 免费导航窗格(HeadingLevel 已内置,零成本)
- PDF 书签:pdfjs-dist 逐页 getTextContent 定位 + pdf-lib 注入大纲,先 spike 1-2 天;红线 = H1/H2 两级最小版或砍
- 设置持久化:手写 userData JSON(少依赖少打包风险)
- 前置底座:标题 slug/id 生成(mdast 标题补 id,TOC/书签/内部链接跳转三者的公共底座)

### 差异化定位
中文排版可控(护城河)+ 真 docx(非 HTML 改名)+ 本地离线隐私

## 语法覆盖范围(转换矩阵)
| 语法 | 支持 | 阶段 |
| ---- | ---- | ---- |
| 标题/段落/粗斜体/行内代码/链接/引用 | ✅ | G1 |
| 无序/有序/嵌套列表 | ✅ | G1 |
| 表格/代码块(等宽字体)/图片(本地路径) | ✅ | G1 |
| GFM 删除线/任务列表 | ✅ | G3 联调期顺手 |
| 脚注/公式/docx 代码高亮/目录 TOC | 延后 | 二期 |

## 选型结论
| 项 | 方案 | 理由 |
| --- | ---- | ---- |
| GUI | Electron(43.x) | 主进程即 Node,现有 TS 管线直接复用;自带 Chromium 一份两用 |
| docx | `docx`(9.x)+ remark 自研渲染管线 | 中文 eastAsia 完全可控(硬需求) |
| pdf | markdown-it + HTML 模板 + `webContents.printToPDF()` | 复用 Electron Chromium,免 puppeteer 双份体积(~300MB) |

## 架构设计
- 分层:
  ```
  src/
    core/            # 纯逻辑,无 IO,可测试
      parse.ts       # remark + remark-gfm → AST
      docx/render.ts # AST → docx
      docx/theme.ts  # 字体/样式集中配置
      pdf/render.ts  # markdown-it → HTML 模板 → printToPDF
      convert.ts     # 格式注册表:convert(md, format, options) => Promise<Buffer>
    main/            # Electron 主进程:窗口、dialog、IPC、执行转换
    renderer/        # GUI UI
    cli/index.ts     # 调试/回归通道(非交付物)
  ```
- 转换在主进程执行(docx 库为 Node 原生;printToPDF 走系统字体,中文零配置)
- renderer:vanilla TS + 原生 DOM(UI 极简,不引前端框架,避免引入构建复杂度)
- IPC:`contextIsolation` + preload 白名单;`invoke('convert', ...)` + 进度经 `webContents.send` 推送;拖放由 renderer 直接取 `File.path` 校验
- 未来扩展格式只需在注册表登记 renderer

## 中文/字体策略
- docx:`theme.ts` 集中配置 `font: { ascii: 'Calibri', eastAsia: '微软雅黑', hAnsi: 'Calibri' }` 挂 Normal 样式;CLI/程序内可覆盖;宋体作备选配置项
- pdf:Windows 系统字体零配置(自动命中微软雅黑);Linux 部署时用 CSS `@font-face` 内嵌 noto-cjk(后置)

## 里程碑(每阶段可独立验证)
| 阶段 | 内容 | 验证 | 状态 |
| ---- | ---- | ---- | ---- |
| G1 core 打底 | remark 管线 + docx 完整渲染(标题/段落/列表/表格/代码块/引用/图片)+ eastAsia 字体;依赖:remark/remark-gfm/docx;临时脚本验证 | 全要素样例生成 docx,打开中文正确无损坏;typecheck 绿 | ✅ 完成 |
| G2 Electron 骨架 | 窗口 + preload + IPC 框架 + 文件选择/拖放 + 格式 UI;依赖:electron | 应用启动,选文件/拖放生效 | ✅ 完成 |
| G3 转换联调 | convert IPC + 进度事件 + 输出落盘 + GFM 删除线/任务列表 | docx 端到端转换成功 | ✅ 完成 |
| G4 PDF 自研 | markdown-it + HTML 模板 + printToPDF + 高亮 + 分页 + 中文验证;依赖:markdown-it/highlight.js | 中文 PDF 正确;结论固化到 docs/研究结论.md | 待启动 |
| G5 收尾 | 错误处理 + electron-builder(NSIS)打包实测 | 安装包可装可跑 | ✅ 完成 |

## 风险与缓解
- Electron 体积(100-250MB):asar + 压缩;不引第二个 Chromium 即最优解
- 国内网络:electron 二进制与 builder 辅助件双镜像(`ELECTRON_MIRROR` / `ELECTRON_BUILDER_BINARIES_MIRROR`),写死勿回退
- printToPDF 中文:与 md-to-pdf 同机制,Win 下微软雅黑零配置;仅 Linux 部署需 @font-face 内嵌
- 打包:ESM 主进程需确认 electron-builder 的 entry/asarUnpack 配置;Windows 路径含空格/编码统一处理
