# 状态速查

## 当前状态
- 2026-08-02:需求变更为 Windows GUI,选型与规划完成(Electron + docx 自研 + printToPDF,见 `docs/路线图与迭代规划.md`);待 G1 开发

## 验证基线
- 首次开发前需安装:`npm install -D typescript @types/node`(走 npmmirror);Electron 需镜像环境变量(见开发者手册)
- 命令固化:`npm run typecheck`、`npm run build`(安装 TS 后实际跑通一次再标记为基线)

## 铁律(勿回退)
- npm 源:npmrc 固定 npmmirror
- Electron 二进制镜像:`ELECTRON_MIRROR` / `ELECTRON_BUILDER_BINARIES_MIRROR`(npmmirror)
- docx 渲染必须走 `core/docx/theme.ts` 集中字体配置(中文 eastAsia),不允许散落硬编码
- pdf 路线固定 Electron `printToPDF`,不引入 puppeteer/md-to-pdf

## 打开事项
- [ ] G1:安装 typescript/@types/node + docx + remark 系依赖,建立验证基线
- [ ] G1:remark 解析 + docx MVP 渲染(标题/段落/强调/列表)+ eastAsia 字体,临时脚本验证中文
- [ ] G4:printToPDF 中文渲染实测,结论固化到 `docs/研究结论.md`
