# 状态速查

## 当前状态
- 2026-08-02:G4 完成(docx + pdf 双格式端到端可用,PDF 视觉验收通过);待 G5(打包)
- 依赖新增:markdown-it 14.3(勿升 15,@mdit/plugin-tasklist peer 冲突)、@mdit/plugin-tasklist、highlight.js

## 验证基线
- 已跑通:`npm run typecheck`、`npm run build`、`node scripts/g1-verify.mjs`、`npx electron . --smoke`(启动 + docx/pdf 双链路 + renderer 诊断)
- smoke 覆盖:docx 全要素、pdf(中文/表格/代码块/任务列表/删除线/图片/页码,魔数校验)、弹窗启动隐藏防回归

## 铁律(勿回退)
- npm 源:npmrc 固定 npmmirror
- Electron 二进制镜像:`ELECTRON_MIRROR` / `ELECTRON_BUILDER_BINARIES_MIRROR`(npmmirror)
- docx 渲染必须走 `core/docx/theme.ts` 集中字体配置(中文 eastAsia),不允许散落硬编码
- pdf 路线固定 Electron `printToPDF`,不引入 puppeteer/md-to-pdf

## 打开事项
- [ ] G5:错误处理收尾 + electron-builder(NSIS)打包实测
- [ ] G4 补充验证:坏路径图片的 PDF 表现(目前无感知,破图占位符)、任务列表缩进目测、标题含 &/< 的文件名(pdf 路线 title 已转义)
- [ ] G2/G3 人工确认:窗口 UI 与 docx 转换实际使用观感
