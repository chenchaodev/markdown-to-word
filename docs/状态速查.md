# 状态速查

## 当前状态
- 2026-08-02:选型与迭代规划完成(docx 自研管线 + md-to-pdf,见 `docs/路线图与迭代规划.md`);docs 结构已按全局模板对齐;待 M1 开发

## 验证基线
- 首次开发前需安装:`npm install -D typescript @types/node`(走 npmmirror)
- 命令固化:`npm run typecheck`、`npm run build`(安装 TS 后实际跑通一次再标记为基线)

## 铁律(勿回退)
- npm 源:npmrc 固定 npmmirror
- docx 渲染必须走 `core/docx/theme.ts` 集中字体配置(中文 eastAsia),不允许散落硬编码

## 打开事项
- [ ] M1:安装 typescript/@types/node + docx + remark 系依赖,建立验证基线
- [ ] M1:remark 解析 + 标题/段落/强调/列表渲染 + eastAsia 字体,生成 docx 人工验证中文
- [ ] M3:md-to-pdf ESM 命名导入实测,结论固化到 `docs/研究结论.md`
