# 状态速查

## 当前状态
- 2026-08-02:G3 完成(convert IPC + 进度事件 + 输出落盘,docx 端到端可用);待 G4(PDF 自研)

## 验证基线
- 已跑通:`npm run typecheck`、`npm run build`、`node scripts/g1-verify.mjs`、`npx electron . --smoke`(启动 + convert 链路自测)
- 依赖:docx 9.x、remark、remark-gfm、electron 43、typescript、@types/node

## 铁律(勿回退)
- npm 源:npmrc 固定 npmmirror
- Electron 二进制镜像:`ELECTRON_MIRROR` / `ELECTRON_BUILDER_BINARIES_MIRROR`(npmmirror)
- docx 渲染必须走 `core/docx/theme.ts` 集中字体配置(中文 eastAsia),不允许散落硬编码
- pdf 路线固定 Electron `printToPDF`,不引入 puppeteer/md-to-pdf

## 打开事项
- [ ] G4:markdown-it + HTML 模板 + printToPDF + 高亮 + 分页 + 中文验证,结论固化到 `docs/研究结论.md`
- [ ] G5:错误处理收尾 + electron-builder(NSIS)打包实测
- [ ] G2/G3 人工确认:窗口 UI 与 docx 转换实际使用观感
