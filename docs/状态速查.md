# 状态速查

## 当前状态
- 2026-08-02:项目脚手架初始化(技术栈:Node.js/TypeScript,ESM);未开始功能开发

## 验证基线
- 首次开发前需安装:`npm install -D typescript @types/node`(走 npmmirror)
- 命令固化:`npm run typecheck`、`npm run build`(安装 TS 后实际跑通一次再标记为基线)

## 铁律(勿回退)
- npm 源:npmrc 固定 npmmirror
- 转换核心库选型确定并验证前,不引入转换依赖

## 打开事项
- [ ] 决定转换核心库(docx / pdfmake / pandoc 绑定)并验证
- [ ] 定义 CLI 参数与输出格式设计
- [ ] 建立验证基线并实际跑通
