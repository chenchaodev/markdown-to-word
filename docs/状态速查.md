# 状态速查

## 当前状态
- 2026-08-02:G2 完成(Electron 骨架:窗口 + preload + IPC + 文件选择/拖放 + 格式 UI);待 G3(转换联调)

## 验证基线
- 已跑通:`npm run typecheck`、`npm run build`、`node scripts/g1-verify.mjs`、`npx electron . --smoke`(启动冒烟)
- 依赖:docx 9.x、remark、remark-gfm、electron 43、typescript、@types/node

## 铁律(勿回退)
- npm 源:npmrc 固定 npmmirror
- Electron 二进制镜像:`ELECTRON_MIRROR` / `ELECTRON_BUILDER_BINARIES_MIRROR`(npmmirror)
- docx 渲染必须走 `core/docx/theme.ts` 集中字体配置(中文 eastAsia),不允许散落硬编码
- pdf 路线固定 Electron `printToPDF`,不引入 puppeteer/md-to-pdf

## 打开事项
- [ ] G3:convert IPC + 进度事件 + 输出落盘,docx 端到端(renderer 转换按钮启用)
- [ ] G4:printToPDF 中文渲染实测,结论固化到 `docs/研究结论.md`
- [ ] G2 人工确认:窗口 UI(拖放/选择文件/格式单选)实际观感
