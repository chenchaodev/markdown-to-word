# 状态速查

## 当前状态
- 2026-08-02:G1 完成(core 转换管线:remark 解析 + docx 完整渲染 + eastAsia 字体);验证基线已建立;待 G2(Electron 骨架)

## 验证基线
- 已跑通:`npm run typecheck`(tsc --noEmit)、`npm run build`、`node scripts/g1-verify.mjs`(生成 output/g1-sample.docx)
- 依赖:docx 9.x、remark、remark-gfm、typescript、@types/node

## 铁律(勿回退)
- npm 源:npmrc 固定 npmmirror
- Electron 二进制镜像:`ELECTRON_MIRROR` / `ELECTRON_BUILDER_BINARIES_MIRROR`(npmmirror)
- docx 渲染必须走 `core/docx/theme.ts` 集中字体配置(中文 eastAsia),不允许散落硬编码
- pdf 路线固定 Electron `printToPDF`,不引入 puppeteer/md-to-pdf

## 打开事项
- [ ] G2:安装 electron(设镜像),窗口 + preload + IPC 框架 + 文件选择/拖放 + 格式 UI
- [ ] G3:convert IPC + 进度事件 + 输出落盘,docx 端到端
- [ ] G4:printToPDF 中文渲染实测,结论固化到 `docs/研究结论.md`
