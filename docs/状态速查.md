# 状态速查

## 当前状态
- 2026-08-08:批次 8「学术正式化延伸」**调研完成、规划细化、暂停待用户决策**(代码未动):exp-1 管线勘察 + lib-1/lib-3 域 API 调研已落盘(研究结论.md + 存档 1123/1142);8a TOC 开关化(原生域已存在,增量=开关+updateFields)、8b 题注 SEQ 编号(STYLEREF+SEQ 域 或 静态注入)、8c Mermaid 升回、8d 公式编号;**开工前需拍板 D1 编号路线(更新域 vs 免更新,8a/8b 必须同路线,严禁混用)与 D2 题注语法**,见路线图批次 8
- 2026-08-08:批次 7「体验优化 + 流程简化」**完成 + 3 个 bug 修复已提交**(24b128b feat / 8e7dfa1 docs / 524cdf2 合并进度条 / fd40480 取消残留 / f809c57 PDF 取消检查点,typecheck/build/smoke 全绿),待用户 GUI 实测(清单 docs/体验优化验收记录.md):列表增删、输出目录、重名加序号、编码预检(GBK 自动转码)、模板回退/非法输入提示、批量导出后一致、快捷键 Ctrl+Enter/Ctrl+O、复制路径、失败弹窗、进度条+取消
- 2026-08-06:批次 6「学术正式化」**完成**(模板包 3a0dc8d + 公式 ea14bd0;GUI 实测跟踪已关闭 2026-08-08,验收产物 `output/批次4验收/07-公式测试.{docx,pdf}`):预设模板(默认/学术论文/商务简报);公式 PDF KaTeX + docx KaTeX MathML→docx Math(OMML 超额交付,降级 TeX 源码兜底)
- 2026-08-06:批次 5 **全部完成**(5a 排版参数化+设置面板 / 5b docx 标题编号+内部链接 / 5c PDF 章节编号+元数据 / raw HTML 白名单,代码+自动化验证全通过;GUI 实测跟踪已关闭 2026-08-08):白名单 14 个无属性内联标签双格式一致渲染,带属性/非白名单标签安全兜底(pdf 转义 / docx 跳过);验收样例 `output/批次4验收/06-raw-html-白名单测试.{docx,pdf}`
- 2026-08-06:批次 5 **5a 完成**(排版参数化 + 设置面板「排版」区块,代码+自动化验证全通过;GUI 实测跟踪已关闭 2026-08-08):字体(西文/中文)/字号/行距/首行缩进 2 字符/两端对齐/章节编号开关 → docx(docx 库实证 `w:sz`/`w:jc both`/`firstLineChars`)+ PDF(模板 CSS 参数化)双格式一致;验收样例 `output/批次4验收/05-排版设置测试.{docx,pdf}`
- 2026-08-06:批次 5 **5c 完成**(PDF 侧章节编号 CSS counter + frontmatter 元数据注入 PDF Info,代码+自动化验证全通过;GUI 实测跟踪已关闭 2026-08-08)
- 2026-08-06:批次 5 **5b 完成**(docx 标题章节自动编号 + 内部/外部链接跳转,代码+自动化验证全通过;GUI 实测跟踪已关闭 2026-08-08):h1-h3 段落级 numbering(静态渲染免 F9,默认开,可关)· [text](#slug) 内部跳转(InternalHyperlink)· 外链真超链接(ExternalHyperlink);验收样例 `output/批次4验收/03-标题编号链接测试.docx`
- 2026-08-05:批次 5「中文排版深化 + 保真补全」**开工**(字体设置/字号行距缩进对齐/章节自动编号/docx 内部链接/raw HTML 白名单/PDF 元数据;规划见路线图)
- 2026-08-05:批次 4 **脚注 + 页眉页脚页码完成**(代码+自动化验证全通过,待用户 GUI 实测):docx 页眉(标题,有 title 才生成)/页脚(第 X 页/共 X 页 PageNumber 域)· docx 脚注(Document 级 footnotes + FootnoteReferenceRun,重复引用各占新 id)· PDF 脚注(@mdit/plugin-footnote + 6 条 CSS,Chromium 下集中在内容末尾渲染);验收样例 `output/批次4验收/02-脚注测试.{docx,pdf}`
- 2026-08-05:批次 4 书签**修复完成**(用户实测:书签目录正常但点击不跳转 → 根因 destKeyText 对 PDFName key 编码解码错误,全部回退首页;已修复并补 smoke Dest 断言);验收样例已重新生成,待用户复测
- 2026-08-05:二期批次 4「长文档」**书签完成**(代码+自动化验证全通过,待用户 GUI 实测):pdf-lib 注入 Outlines(读 /Dests 命名目标 → 中文 UTF-16BE 大纲树,单文件+合并共用);验收样例 `output/批次4验收/01-简介-合并.pdf`(18 条书签,嵌套层级正确)
- 2026-08-04:二期批次 3「批量 + 合并」完成且**用户实测通过**(拖放取路径修复 + 文件排序 + 批量/合并转换);实测反馈:PDF 侧边栏书签为空(页面内目录正常)→ 批次 4 开工,**书签优先**
- 2026-08-04:二期批次 3「批量 + 合并」完成(代码+自动化验证全通过,待用户 GUI 实测):批量转换(多选/拖放文件夹 + 队列并发 2 + 失败逐条汇总弹窗)· 多文件合并(frontmatter 仅首个 + 图片转绝对 + page-break 拼接);验收样例 `output/批次3验收` 目录
- 2026-08-03:二期批次 2「保真 + 正式文档化」完成且**用户验收通过**(Word/WPS 更新域生成目录 + PDF 目录点击跳转实测通过);批次 3 规划见路线图
- 2026-08-03:二期批次 2「保真 + 正式文档化」完成(代码+自动化验证全通过,待用户 Word/WPS 实测):外链图片下载嵌入 · TOC(docx 域 / PDF 锚点目录)· 封面页 + YAML · PDF 预览;验收样例 `output/批次2验收.{md,docx,pdf}`
- 2026-08-03:二期批次 1「排版控制 + 设置底座」完成且用户验收通过(Word/WPS 实测 + GUI 试用);批次 2「保真 + 正式文档化」开工(外链图片/TOC/封面+YAML/PDF 预览)
- 2026-08-03:二期批次 1「排版控制 + 设置底座」完成(代码+自动化验证全部通过,待用户 Word/GUI 实测):设置持久化/导出后行为/分页控制(H1 分页+显式分页符)/页面设置面板(纸张/方向/边距)/标题 slug 底座
- 2026-08-02:G5 完成(docx + pdf 双格式端到端可用 + NSIS 安装包实测可装可跑);MVP 里程碑全部完成
- 初稿完成:G1-G5 全部里程碑达成(docx/pdf 双格式、错误处理、NSIS 打包、图标);二期按三批交付,规划见 `docs/路线图与迭代规划.md`
- 依赖新增:markdown-it 14.3(勿升 15,@mdit/plugin-tasklist peer 冲突)、@mdit/plugin-tasklist、@mdit/plugin-footnote(1.0.2,peer 显式 markdown-it ^14.2.0)、highlight.js、electron-builder 26.15.3(勿用 27 alpha)

## 验证基线
- 已跑通:`npm run typecheck`、`npm run build`、`npx electron . --smoke`(启动 + docx/pdf 双链路 + 设置持久化/landscape 端到端 + 批量/合并端到端 + renderer 诊断)
- 批次 7 修复回归(524cdf2/fd40480/f809c57):typecheck/build/smoke 全绿;smoke 现自清理 output 产物(批次 7 重名保护后旧产物不再被覆盖,否则断言 endsWith("-合并.docx") 因 (N) 序号变体失败;Windows 占用文件 EBUSY 容错跳过)
- 批次 7 验收断言(make-batch4-sample.mjs 第 8 段):编码预检 decodeMarkdown(UTF-8 无 BOM/带 BOM/UTF-16LE BOM/GBK 文件按 gb18030 解码)解码与标记全部正确
- 批次 7 其余 main 侧行为(重名序号/输出目录/取消/批量导出一致)走 smoke + GUI 实测清单(验收脚本无法直接触达 main IPC 层)
- 批次 3 smoke 断言:批量 3 成功 1 缺失(汇总逐条正确)+ 合并 docx(frontmatter 仅首个/图片嵌入/两文件标题齐全)
- 批次 3 实测补充:拖放取路径 `webUtils.getPathForFile`(File.path 已移除);文件列表排序(拖拽 + 上移/下移)
- 批次 4 smoke 断言:单文件 pdf + 合并 pdf 读回 Outlines,中文标题 UTF-16BE 解码正确(覆盖「侧边栏书签为空」回归)
- 批次 4 脚注/页眉页脚断言(make-batch4-sample.mjs):docx 解包含 footnotes.xml/footer1.xml/header1.xml(metadata.title 存在时);pdf html 含 footnotes 区 + footnote-ref 上标引用
- 批次 5b 断言:docx 解包 numbering.xml 含多级 text 模板 %1/%1.%2(indent 360/360);document.xml 含 w:hyperlink w:anchor 指向标题书签 + 书签仍在(编号不破坏 Bookmark)
- 批次 4 前置实测:合并 PDF 产物含 18 个 Link 注释 + /Dests 命名目标(锚点链接存在),但**无书签大纲**(printToPDF 不产出)→ 需 pdf-lib 后处理注入;实测为**旧式直接 /Dests 字典**(非名称树),lookupNamedDest 双结构兼容
- 双格式渲染断言(临时脚本,17 项):分页符/书签/pageBreakBefore/pgSz(pdf 版见提交内 `.verify-tmp` 已删,重验可写临时脚本调 dist/core)
- 分页符 PDF 确定性验证:两行字+分页符 → printToPDF 产物 /Count=2
- 打包:`npm run dist`(electron-builder NSIS);验证链:--dir → asar list → win-unpacked 启动存活 → 静默安装/卸载(退出码 0)
- 注意:打包版 `--smoke` 不可用(asar 内只读,output/ 写不进);镜像环境变量见开发者手册

## 铁律(勿回退)
- npm 源:npmrc 固定 npmmirror
- Electron 二进制镜像:`ELECTRON_MIRROR` / `ELECTRON_BUILDER_BINARIES_MIRROR`(npmmirror)
- docx 渲染必须走 `core/docx/theme.ts` 集中字体配置(中文 eastAsia),不允许散落硬编码
- pdf 路线固定 Electron `printToPDF`,不引入 puppeteer/md-to-pdf
- 显式分页符语法固定 `<!-- page-break -->`(不占 `---` 的 hr 语义);docx landscape 尺寸传原始(纵向)值,勿手动交换(docx 库自动交换)

## 打开事项
- [ ] **批次 8「学术正式化延伸」暂停中,等用户拍板后再开工**(规划见路线图批次 8,调研已落盘):
  - **D1 编号路线**(8a/8b 必须同路线,严禁混用):① 更新域路线(推荐,Word 打开弹一次提示全量更新,F9 同步;8a `features.updateFields:true` + 8b STYLEREF+SEQ,与 Word 原生题注同构;WPS 兼容需实测)② 免更新路线(零提示全端一致,但改标题后需重新导出;8a `beginDirty:false`+cachedEntries 纯超链接 + 8b 静态注入「图 1.1」文本)
  - **D2 题注语法**:候选为图/表段落后紧跟「图: 标题」前缀行识别(alt 已被占位文本复用,倾向前缀行;PDF 侧 `.fig-caption::before { content: counter(h1c) "." counter(figc) }`)
  - 8a 现状:docx 原生 TOC 域已存在(render.ts:289-309 TableOfContents 组件),增量=开关化 + updateFields 联动;8b 需预扫建题注上下文(仿 footnoteNextId);8c Mermaid 升回(Chromium 有 DOM);8d 公式编号(REF 用 NumberedItemReference/Bookmark)
- [ ] 批次 7「体验优化」:完成待用户 GUI 实测(清单见 docs/体验优化验收记录.md 待办):①列表增删(移除/追加/清空)②输出目录选择与恢复默认③进度条+取消(合并进度条 bug 已修 524cdf2、取消后重试秒失败已修 fd40480、PDF 取消不生效已修 f809c57,待复测)④重名自动加序号(连续转两次同名文件)⑤GBK 编码文件自动转换⑥Ctrl+Enter/Ctrl+O 快捷键⑦复制路径按钮⑧失败弹窗三要素