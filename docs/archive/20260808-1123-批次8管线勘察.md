# 批次 8 前置勘察报告(docx TOC 域 + 题注 SEQ 域)

来源: @explorer exp-1(ses_020cde269ffe69XSWZ20X3u9p4),2026-08-08-1123
勘察对象: 批次 8「Word 原生 TOC 域 + 图/表题注自动编号 SEQ 域」实施前置
注意: 本文件仅供自查,摘要见 docs/研究结论.md;exp-1 只读勘察,以下行号基于当时工作区

## 1. AST 形态(src/core/parse.ts)
- 引擎: `remark().use(remarkGfm).use(remarkMath).parse(md)` 产出 mdast(parse.ts:24-29);remark-gfm 提供 table/delete/任务列表;remark-math 提供 inlineMath/math
- 标题 id/slug: 解析后立即为所有 heading 写 `node.data.id`(parse.ts:31-41 walkHeadings),文本取全部 text 子节点拼接(parse.ts:43-51 collectText);`mdast.Data` 已声明合并 `{ id?: string }`(parse.ts:11-15,注释「公共契约,勿移除」)
- slug 算法(src/core/slug.ts): slugify()(8-15)保留 `\p{L}\p{N}_-`,空白转连字符,其余删除,空回退 "section";uniqueSlug()(18-23)seen Map 追加 -2/-3;docxBookmarkId()(29-33)数字开头加 h- 前缀、≤40 字符截断
- 图片: mdast `image` 属 PhrasingContent(行内,在段落内),字段 url/alt/title(render.ts:104-105 注释)
- 表格: mdast `table` 块节点,children = tableRow → tableCell(render.ts:496-520)
- **无 caption 类节点**: 全库 grep caption|figcaption|figure|题注 零命中;`<figure>/<figcaption>` 不在 raw HTML 白名单(docx 跳过、pdf 转义)

## 2. docx 章节编号现状(render.ts)
- **实现: 段落级 numbering 静态渲染,不是域**。h1-h3 挂 `numbering: { reference: "md-heading", level: node.depth - 1 }`(render.ts:425-428),编号由 Word/WPS 打开时按 numbering 定义自动计算,**无需 F9**(render.ts:135 注释)
- headingNumberingOptions()(render.ts:136-152): 3 级 level 0/1/2,format "decimal",textFor 模板 `%1`/`%1.%2`/`%1.%2.%3`,start 1,缩进 left/hanging 360;注册 Document: render.ts:227
- 计数器由 OOXML numbering 引擎管理(多级自动重置),**代码无计数器变量**;开关 `ctx.headingNumbering`(render.ts:166,默认 typography.headingNumbering);入口 renderHeading()(render.ts:409-435)
- **不能直接复用拼题注前缀**: numbering 按段实例计数,题注挂 md-heading 会破坏标题计数;无 API 读「当前章节编号值」注入文本;标准解法 STYLEREF 域(见建议)

## 3. docx TOC 现状(render.ts)
- **原生 TOC 域已存在**: renderTocPage()(render.ts:289-309)正文含任意 heading 时**无条件插入**(render.ts:185-187,无开关;短文档无标题才不生成)
- 实现: docx 9.x `TableOfContents` 组件(import render.ts:22;children 含它 render.ts:179),配置 `{ hyperlink: true(\h), headingStyleRange: "1-3"(\o "1-3"), useAppliedParagraphOutlineLevel: true(\u), hideTabAndPageNumbersInWebView: true(\z) }`(render.ts:296-300),占位 `"(目录:请右键 → 更新域 生成)"`(render.ts:301-305);前有「目录」标题(36 half-points 居中,render.ts:291-295),后有 PageBreak(render.ts:307);目录标题用普通 Paragraph 避免被 \o "1-3" 自收集(render.ts:285-286)
- 全库仅此一处域: grep TableOfContents|SimpleField|ComplexField|Seq|SEQ|fldChar|instrText|STYLEREF 只命中 render.ts 5 处;页脚 PageNumber.CURRENT/TOTAL_PAGES(render.ts:328-330)与 FootnoteReferenceRun(render.ts:629)是仅有其他「域类」元素
- 无 `[TOC]` 语法处理: markdown-it/remark 侧均无 TOC 指令解析;docx TOC = 有标题即自动生成,PDF 侧见第 5 节

## 4. 图片/表格渲染路径(docx)
- 图片: image(行内)→ pushRuns case "image"(render.ts:621-623)→ imageToDocx()(render.ts:741-766): ctx.imageResolver(node.url) 读 Buffer(753),失败 → 占位灰字 `[图片: alt]` + 外链警告(742-746、757-760);成功 → ImageRun { type: sniffImageType(data), data, transformation: { width: 400, height: 300 } }(761-765)——**固定 400×300,不读元数据不保纵横比**;sniffImageType()(771-782)魔数判 png/jpg/gif 未知回退 png;同步场景降级 `[图片: alt]` 文本(700-702 pushRunsSync)
- 表格: table → renderBlock case "table"(359-360)→ renderTable()(496-520): 遍历 rows → 单元格 renderPhrasing(normalizeInlineHtml(...), rowIndex===0 ? { bold: true } : {})(502),首行加粗;Table 宽度 100% PERCENTAGE,全边框 SINGLE size 4 黑(507-519)
- **题注插入点**: 图片是段落内行内节点 → 图题注只能在块级插入——renderBlock "paragraph" case(352-356)渲染完含图段落后追加;表格在 "table" case(359-360)前后插入;**当前无 AST 位置跟踪**,renderBlock 逐节点 switch 无索引上下文;若需「图 N」需文档级计数 ctx 字段(参考 footnoteNextId 模式 render.ts:87、169)或预扫

## 5. PDF 侧现状(pdf/render.ts)
- 无 template.html 文件(仅 render.ts/metadata.ts/bookmarks.ts);模板内联字符串 buildTemplate()(406-419)
- 标题编号已实现: CSS counter——buildTemplateCss() 追加 `body { counter-reset: h1c h2c h3c; }` + 各标题 counter-increment/reset + `h1/h2/h3::before { content: counter(h1c) "." ... }`(369-377,开关 headingNumbering);编号经 ::before 伪元素**不进 HTML 文本节点**,书签/目录文本不受影响(239-240 注释);PDF 题注可轻易实现: `.fig-caption::before { content: counter(h1c) "." counter(figc) }`(伪元素)
- 目录现状: buildTocHtml()(483-495)从渲染后正文**正则提取 h1-h3**(extractHeadings,467-475)生成无页码锚点列表 `<div class="toc">`,拼接在封面后正文前(588);无 [TOC] 语法,有标题即自动生成;锚点 id 由 overrideHeadingIdRule()(222-232)注入 uniqueSlug
- 图片: markdown-it 内置 image 规则 + overrideImageRule()(204-216)相对路径转 file://;渲染后 embedExternalImages()(505-541)http(s) 图并发(限 3)下载为 data URL,失败保留原 URL + 警告;CSS `img { max-width: 100% }`(326)
- 表格: markdown-it 内置 table,样式 table/th/td + 斑马纹(317-324),无 caption;markdown-it 侧未挂 TOC 语法插件(只挂 tasklist/footnote/katex,87-91)

## 6. 选项流(convert → render 全链路字段)
- core 入口: convert(md, format, context)(convert.ts:107-148),ConvertContext(38-55): baseDir/imageResolver?/title?/warnings[]/pageSetup?/typography?/breakBeforeH1?/katexDir?;renderDocx(ast, {...})(138-147)与 renderPdfHtml(body, {...})(121-134)
- docx 侧 RenderOptions(render.ts:54-70): imageResolver/metadata/title/warnings/pageSetup/typography/breakBeforeH1/headingNumbering(显式优先,否则 typography.headingNumbering)
- PDF 侧 RenderPdfHtmlOptions(pdf/render.ts:27-52)同构 + katexDir
- 主进程: convertImpl() 每次实时 loadSettings()(main/index.ts:97)传 pageSetup/typography/breakBeforeH1(103-114);mergeConvertImpl 同(241-250);预览同(319-327)
- 设置模型: AppSettings(settings.ts:16-26) = version/format/pageSetup/typography/breakBeforeH1(顶层布尔,顶层开关先例)/afterConvert;TypographySettings(typography.ts:7-22)含 headingNumbering(设置项先例,挂 typography 内);sanitize 白名单在 settings.ts:119-152,**新增字段需同步改 SETTING_KEYS(settings.ts:45) + sanitize + renderer 侧平行类型/默认值(renderer.ts:98-105、131-153)**
- 结论: 新开关两处可选——顶层 breakBeforeH1 模式(独立布尔)或 typography.headingNumbering 模式(嵌套布尔);TOC 与题注编号若需「独立于排版的文档级开关」仿 breakBeforeH1 放顶层;题注前缀开关可挂 typography 内,但 PDF 侧基于 CSS counter、docx 侧基于 numbering,双格式参数需各自消费

## 7. 验收脚本钩子(scripts/make-batch4-sample.mjs)
- 运行: `npx electron scripts/make-batch4-sample.mjs`(需已 build,import dist/core/*;12-28 行);**批次 8 新增段 append 到 app.whenReady() try 块内**(第 89 行起),落盘段 426-438 格式照抄
- 断言手段:
  - docx 部件存在性: zipContains(buffer, "word/footnotes.xml")(48-50)——zip 明文中央目录 buffer.includes()
  - docx 部件内容: unzipPart(buffer, "word/document.xml")(53-63)——tar -xf 解包读文本 + 字符串匹配 OOXML 片段,如 w:sz w:val="28"(203)、w:jc w:val="both"(209)、w:numPr 缺失断言(212-214)、w:hyperlink w:anchor(336)、w:bookmarkStart(340)、w:lvlText w:val="%1.%2"(332)、<m:oMath(373)
  - PDF: convert(md,"pdf") 产物 .html 字符串匹配(class="katex"、@font-face、counter(h1c)、text-indent: 2em,166/227/390-395);必要时 htmlToPdf()(66-87)真实 printToPDF + PDFDocument.load 读回元数据(170-175)
- 现有 8 段: 1) 合并 PDF 书签注入(91-109);2) 脚注+页眉页脚部件(111-162);3) 标题编号+内部/外部链接(308-344);4) PDF 章节编号+元数据(164-177);5) 排版设置双格式(179-234);6) 内联白名单双格式(236-306);7) 公式双格式(346-398);8) 编码预检(400-424)。批次 8 建议新增「9) TOC 域 + 图/表题注 SEQ 域」段

## 题注插入点建议(docx)
- renderDocx 预扫阶段(仿脚注预扫 render.ts:174-178)建「文档级题注上下文」——顺序遍历 ast.children 为每个含图段落/表格分配 { 类型, 序号, 章节上下文 };渲染在 renderBlock "paragraph" 与 "table" case(352-360)内、产出图/表节点后追加题注段落
- 编号前缀「图 1.1」章节部分: 标题是 w:numPr 而非域,题注段落**无法复用标题计数器**;(a) STYLEREF 域取最近 Heading 1 编号值 + SEQ 域计数(Word 原生标准做法,但**需更新域**才能显示,与现状「无需 F9」冲突);(b) 渲染期**静态注入**当前章节号文本 + SEQ 域仅承担图序号(章节号随重排不自动更新,但免 F9)。建议先定「是否接受需更新域」的产品决策

## TOC 注入点建议
- docx 侧原生 TOC 域已存在(render.ts:289-309),批次 8 增量 = **开关化**: RenderOptions/设置模型加 toc?(默认开),render.ts:184-187「有标题即插目录」改受开关控制;确认 \o "1-3" 是否覆盖题注(题注若用标题样式会被收集,需 \b 排除或题注用非标题段落样式)
- PDF 侧 buildTocHtml(483-495)同理加开关,题注计入/排除规则与 docx 对齐
