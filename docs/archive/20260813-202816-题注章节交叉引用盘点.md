# 题注/章节交叉引用盘点(2026-08-13 20:28:16)

> 来源:@explorer exp-1(题注/章节引用盘点,只读代码盘点)。关联:docs/RESEARCH.md 2026-08-13 条目;docs/ROADMAP.md 9d;docs/ACCEPTANCE.md 批次 10 功能 2。

## 1. 题注渲染现状(8b)

**docx 侧**(`src/core/docx/captions.ts`,88 行):
- 识别:`buildCaptionContext(ast, ctx)`(captions.ts:40-70)renderDocx 预扫(render.ts:215);`isCaptionTarget`(:27-29)判定「表格块/含图片段落」,其后题注段 `/^(图|表)[:：]\s*(.*)$/s`(:58),collectPlainText 取整段
- 编号:**预扫局部变量不入 ctx**——chapter(h1 计数,:43/:49-53)、figIndex/tabIndex(h1 处重置,:44-45/:51-52);命中题注才 ++index(:61,无题注的图/表不占号);`CaptionInfo { type, chapter: number|null, index, text }`(:7-15),chapter null 条件 `!ctx.headingNumbering || chapter===0`(:64)
- 插入:renderBlock paragraph case(render.ts:412-423)→ renderCaptionParagraph(captions.ts:73-85)居中段落静态文本「图 3.1 标题」,**替换原题注段**
- 前缀剥离:docx 侧 text: match[2](:66)

**pdf 侧**(src/core/pdf/render.ts):
- 识别:overrideCaptionRule「caption_recognize」(render.ts:119-155)token 流跟踪容器深度,paragraph_close 前 inline 首 text token `/^(图|表)[:：]\s*/`(:136),前驱 table_close 或含图段落(:140-148),剥前缀(:150),class 挂 tokens[i-2](paragraph_open,:151)→ `<p class="fig-caption">`
- 编号:CSS counter 伪元素不进文本(template.ts:167-180,`counter-reset: figc tabc` + `.fig-caption::before { content: "图 " counter(h1c) "." counter(figc) " " }`;headingNumbering 关/无 h1 → 纯序数 :176-179)

**扩展点**:题注 label 后缀(`图: 标题 {#fig:label}`)需改:docx captions.ts:58-67 match 后追加剥离(参考 equations.ts:44 `\{#eq:([\w-]+)\}$`)+ 登记 Map;pdf render.ts:150 剥前缀后追加 + 锚点插入(class 已定位 tokens[i-2])。
**现成先例(eq-label 逐字)**:docx 书签 `bookmarkChildren(ctx, docxBookmarkId("eq-"+label), runs)`(render.ts:482,定义 :397-400,ctx.bookmarkNextId.value++ 保 w:id 唯一);pdf 锚点 `<span id="eq:${label}">`(render.ts:259)。

## 2. 标题渲染与编号现状

**docx**:
- renderHeading(render.ts:511-538);编号 `numbering: { reference: "md-heading", level: node.depth-1 }`(:527-530,depth≤3),numbering 定义 headingNumberingOptions(render.ts:166-182,text %1/%1.%2/%1.%2.%3)——**编号由 Word 引擎生成,document.xml 无编号文本**
- 书签:标题 id 由 parse.ts 预扫生成(walkHeadings collectText + uniqueSlug 挂 node.data.id,:33-38);renderHeading bookmarkChildren(docxBookmarkId(id), runs)(:533-536),任意 depth 都挂;内部链接 [x](#slug) → InternalHyperlink anchor=docxBookmarkId(render.ts:720-729)

**pdf**:
- 编号:CSS counter 伪元素(template.ts:158-166),不进文本,extractHeadings 取不到(postprocess.ts:16-24 正则 /<h([1-3])[^>]*id="([^"]+)"[^>]*>(.*?)<\/h\1>/g)
- 标题 id:overrideHeadingIdRule(render.ts:353-363)uniqueSlug(text, seen) 挂 id;书签 PdfHeading{level,id,text}(bookmarks.ts:34-38),书签名 = slug 与 /Dests key 对应(:219-236)

**扩展点**:标题 label 需改三处:① parse.ts:33-38 slug 生成前剥离(否则进 slug 和标题文本);② docx renderHeading runs 剥离(:520);③ pdf overrideHeadingIdRule text 剥离(:357)。
**拿「当前章节号」docx 侧可行**:captions.ts 已有 h1 计数先例(:49-53);推广 h1/h2/h3 三计数器(镜像 %1.%2.%3 重置语义)即可渲染期静态拿「3」「3.2」;编号值须与 Word numbering 语义严格一致(h1 增→h2/h3 清零,h2 增→h3 清零;headingNumbering 关无号)。

## 3. 公式交叉引用全链路(免更新先例)

- label 行:docx 预扫 equations.ts:43-55(顶层段落 collectPlainText 匹配 /^\{#eq:([\w-]+)\}$/,登记 labelIndex Map + skipSet,前无公式警告 :52);pdf core 规则 render.ts:169-262 第一遍(:193-216,三 token hidden + 清空 inline.children :211-214——markdown-it 不检查 inline 自身 hidden 的坑)
- 引用链接:docx pushRuns link case /^#eq:([\w-]+)$/(render.ts:694-719);pdf 第二遍遍历 inline children 找 link_open href 前缀(:219-247)
- docx:label 行不渲染 skipSet(render.ts:414);书签 eq-${label}(:480-483,bookmarkNextId);引用 text==="式"/"公式" → `式 (${n})` + InternalHyperlink(:699-711),其他文本保持原样仍跳转(:712-718);编号查表 ctx.equationLabels(render.ts:219)
- pdf:锚点包 math_block 渲染规则(render.ts:250-261,`<div class="eq-block">${anchor}${html}<span class="eq-num">(${eqIndex})</span></div>`);引用替换 child.content==="式"/"公式" → `${content} (${num})`(:240-242);悬空 docx `式 (?)` + warnings「交叉引用未找到公式 label」(render.ts:707-709)、pdf `式 (?)` + warnings「引用未定义的公式标签」(render.ts:231-234,env 注入 :385)
- **可复用**:通用骨架 = label 识别/剥离、label→编号 Map、docx 书签+bookmarkNextId、pdf 锚点、引用文本替换、悬空(?) + warnings 双端
- **eq 特化(需参数化)**:① 硬编码「式/公式」文本(:240-241、render.ts:699);② label 命名 eq-${label};③ 编号来源=公式计数器;④ label 语法为独立行(题注/标题是行内后缀,识别位置不同);⑤ docx label 行跳过 skipSet(题注/标题的 label 剥在文本内)

## 4. 编号获取架构约束

- docx 标题编号 Word 引擎生成 → 交叉引用「图 3.2」挂章节号需自维护「当前章节号」:可行有先例(captions.ts h1 计数、equations.ts 连续计数、footnoteNextId);扩展 h1/h2/h3 三计数器镜像 numbering 语义;渲染顺序=文档顺序与 Word 引擎一致(前提:所有 h1-h3 都挂 numbering、无跳过层级——现状满足)
- 风险:编号值只是镜像,改 numbering 配置会漂移——与 8b 题注章节号同一性质,可接受(免更新路线「改号重新导出」)
- pdf:CSS counter 伪元素渲染期拿不到值 → 两条路:① 仿 eq_numbering 在 core 规则镜像 counter 逻辑(推荐,与 CSS counter 双实现同步是 8b 已知边界);② printToPDF 前 executeJavaScript 读 getComputedStyle ::before content(路径长,不建议)

## 5. 测试基建

- eq-numbering.test.js 9 断言:docx 书签 w:id 唯一性(正则收集去重,:39-46)、静态编号(:48-50)、制表位(:52-54)、书签名(:56-58)、引用文本+anchor(:60-62)、label 不渲染(:64)、悬空 (:65-68);pdf 断言 class/id/href(:74-88)
- toc-caption.test.js:docx「图 1.1 总体架构示意图」(:64-66)、孤立前缀行(:68-70)、captionNumbering:false 原文(:77-83);pdf fig-caption(:88-93)、CSS counter(:95-97)
- heading-links.test.js:书签唯一性(:48-55)、numbering %1(:58-60)、w:anchor(:62-64)、paragraphXmlAt 定位辅助(:114-119 可复用)
- 新段 cross-ref.test.js 仿照:convert → unzipPart → 断言静态文本/书签名/anchor/w:id 唯一性;pdf html 断言;warnings;saveArtifact("cross-ref")(artifacts.js:13-21)

## 交叉引用通用骨架(设计建议)

| 维度 | 公式(先例) | 题注 | 标题 |
|---|---|---|---|
| label 语法 | 独立行 {#eq:label} | 行内后缀 {#fig:label} | 行内后缀 {#sec:label} |
| 编号来源 docx | 自维护连续计数 | CaptionInfo.chapter/index 已有 | 新增 h1/h2/h3 计数器 |
| 编号来源 pdf | data-eq-index | 镜像 figc/tabc/h1c | 镜像 h1c/h2c/h3c |
| 锚点 docx | eq-+bookmarkChildren | fig-/tab- 前缀 | 标题已有 slug 书签,sec label 映射/别名 |
| 锚点 pdf | span id=eq: | span id=fig: | 已有 id=slug,加别名锚点 |
| 引用替换 docx | 「式/公式」特判 | 泛化前缀表 | 泛化前缀表 |
| 悬空 | (?) + warnings 双端 | 同 | 同 |
| 开关联动 | — | captionNumbering | headingNumbering(无号时引用显示需定义) |

**落地**:① 双端各建 label 注册表 Map<label, {kind, number}>;② 命名空间前缀 eq-/fig-/tab-/sec- 防冲突,统一 docxBookmarkId 兜底;③ 引用替换泛化为 kind→前缀文本表,未知前缀保持原样跳转;④ 题注/标题 label 剥离两处(显示文本 + docx slug 生成 parse.ts);⑤ pdf 编号计算与 CSS counter 双实现同步是已知边界,验收清单标注。
