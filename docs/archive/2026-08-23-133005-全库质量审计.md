# 全库质量审计(代码 / 文档 / 可用性)

> 时间戳:2026-08-23 13:30:05
> 背景:1.0.0 发版后的整体改进点盘点,主会话派遣 3 个 explore 子代理并行深审(src/core、src/main+renderer、测试体系+工程配置),主会话复核文档与关键论断抽查。纯研究,未改任何文件。
> 用途:后续改进迭代的规划依据(证据链);结论摘要见 RESEARCH.md 同日条目。

---

# 一、src/core/ 质量审查报告(子代理 1)

**范围**:`src/core/` 全部 28 个 TS 文件,约 4440 行。

## 维度 1:代码坏味道

### 过长函数(>150 行)
| 位置 | 行数 | 说明 |
|---|---|---|
| `docx/render.ts:845-1010` (`pushRuns`) | ~165 | `link` 单 case 占 ~97 行(874-971),混杂 5 种职责:公式引用替换、fig/tab/sec 交叉引用、内部锚点、外链、假链接降级 |
| `docx/render.ts:232-391` (`renderDocx`) | ~160 | ctx 组装 + 5 轮 AST 预扫(脚注/章节号/公式/题注/目录)+ 页面设置 + Document 组装,预扫块可提取 |
| `pdf/render.ts:370-531` (`overrideXrefRule`) | ~160 | 内嵌 core rule 第一遍计数/剥离/登记 + 第二遍链接替换 + renderer 包装三段 |

### 重复逻辑
- **`CROSS_REF_KINDS` 逐字复制两份**:`docx/render.ts:157-161` ↔ `pdf/render.ts:308-312`,靠人肉纪律维持,应抽 `core/cross-ref.ts` 单源。
- **`\s*\{#sec:[\w-]+\}$` 正则至少 4 处副本**:`parse.ts:24`(SEC_LABEL_RE)、`docx/render.ts:671`(stripTrailingSecLabel)、`docx/render.ts:685`(stripSecLabelSuffix)、`pdf/render.ts:325`(动态构造)。
- **容器深度跟踪在 pdf/render.ts 重复 3 次**:`caption_recognize`(153-158)、`eq_numbering`(210-215)、`xref_recognize`(385-390)——blockquote/list_item/table_cell 三计数器完全同构,应提取 `createDepthTracker()`。
- **第二遍链接替换循环同构**:`pdf/render.ts:254-282`(eq)与 `459-517`(xref)。
- **白名单双扫描器**:`matchAllowedHtmlExpression`(pdf/render.ts:539-569)与 `isAllowedInlineHtml`(html-whitelist.ts:20-45),规则细节已漂移(见维度 4 大小写不对称)。
- **`ImageResolver` 类型独立声明 3 处**:docx/render.ts:67、pdf/render.ts:28、convert.ts:38。
- **`decodeEntities` 双实现**:utils.ts:18 与 docx/code-highlight.ts:49。
- **`bookmarkChildren` 模式重复**:docx/render.ts:494-497 与 docx/captions.ts:119-123(后者注释称内联避免运行时循环——helper 可放不依赖 render.ts 的位置)。

### 魔法数字/字符串
- docx/render.ts 硬编码颜色 14 处(`"888888"`×8、`"808080"`×3、`"F2F2F2"`、`"999999"`);theme.ts 定义了 `QUOTE_COLOR` 却无人使用。
- **theme.ts 死导出**:`DEFAULT_FONT`/`DEFAULT_SIZE`/`QUOTE_COLOR` 全仓库零引用;正文默认字体实际已迁至 typography.ts,与 theme.ts「集中字体配置」硬约束定位矛盾——删或明确兜底角色。
- 封面字号 44/22(render.ts:409,417,426)、目录 36(445)、页眉页脚 14(464,475-479)等 half-point 值散落;`scaleToFit` 上限 400(1042-1044)与图片兜底 400×300(1082)裸数字。

### 类型断言
- `as unknown as ParagraphChild[]` ×2(docx/render.ts:496、captions.ts:122,d.ts 限制已注明——应共用 helper)。
- render.ts:875 提取链接文本可复用 `collectPlainText(node)` 消断言。
- bookmarks.ts:193 断言后紧跟 instanceof 校验,断言冗余。
- **无 `as any`**,断言纪律良好。

## 维度 2:错误处理
总体:「降级 + 警告」模式贯彻好(公式/mermaid/webp/图片失败均降级不中断)。缺口:
- **pdf/template.ts:213-215 `loadKatexCss` 静默吞错返回 ""**:katexDir 配错 → 公式无字体零提示;函数签名无 warnings 通道。
- **docx/render.ts:1069-1071 `catch { data = null }` 吞异常原因**:权限拒绝与不存在不可区分,warnFail 只有统一文案(image-warning.ts:11)。
- pdf/postprocess.ts:66-69 同样吞原因。
- **hljs 高亮降级无警告**(code-highlight.ts:97-99,136-138;pdf/render.ts:115-117):mermaid 降级有警告而高亮没有,用户不知道为何没颜色。
- **pdf/metadata.ts:25 日期解析失败静默兜底当前时间**:误导归档检索;更合理是不设置。
- **警告去重不一致**:pdf 侧 unknownLabels Set 去重(pdf/render.ts:267,495),docx 侧悬空 label 每次引用都 push(895,944)——GUI 警告列表刷屏。
- **警告文案全部硬编码中文**(ctx.warnings 所有消息):i18n 只覆盖 UI,英文界面用户转换警告仍是中文。

## 维度 3:性能
- **[最主要] docx 图片无去重缓存**:render.ts:1054-1084 每个 image 节点单独调 resolver,重复图片 N 次 IO + N 份 Buffer;pdf 侧已有 Set 去重实践(postprocess.ts:63、87)。修复 ~10 行(ctx 挂 memo Map)。
- `checkLocalImages` 为存在性检查读全量 Buffer(resolver 契约所致)。
- `embedExternalImages` 逐 URL 全串 replace(postprocess.ts:110-115)O(n·m) 且 base64 全拼单串;可仿 replaceMermaidPlaceholders 的 cursor 分段方案(649-685)。
- normalizeInlineHtml 理论 O(k²) 实际可忽略;buildMarkdownIt 每次新建可复用但桌面单发无所谓;未发现真正病态 O(n²);并发安全(postprocess worker-pool 单线程安全,ctx 无跨文档泄漏)。

## 维度 4:正确性隐患
- **[中危] frontmatter 误吞以 `---` 开头的正文**(frontmatter.ts:24):正则把任何 `---\n...\n---` 开头当 frontmatter 剥离,夹在中间的文字从 body 静默消失且 metadata 为空。建议:仅当块内命中至少一个已知 key(title/author/date)才认定。
- **[中危] docx 书签名 40 字符截断碰撞**(slug.ts:32):`id.slice(0,40)` 在 uniqueSlug 去重之后,共享前 40 字符的两个长中文标题产出相同 w:name → Word/WPS 跳转错位。建议截断场景追加短哈希。
- **[中危] 列表项/引用块内不支持的块级内容静默丢弃**:renderList(692-712)只处理 paragraph/list/code/blockquote——列表项内 display 公式/表格/html 无声消失;renderBlockquote(757-773)同样丢代码块。内容丢失类缺陷至少 push warnings。
- **[中] GFM 表格列对齐 docx 侧丢失**(renderTable 775-799 忽略 mdast table.align):`:---:` 在 Word 全左对齐,pdf 侧原生支持——双格式保真不一致。
- **[中] headingNumbering=false 时题注编号双格式分歧**:captions.ts:60-66 每个 h1 无条件重置 figIndex/tabIndex(chapter 因开关关闭记 null)→ docx 输出按章重置的「图 1」「图 1」;pdf/render.ts:395-405 只在 isNumbered 时重置 → PDF 连续「图 1」「图 2」。**captions.ts 自己的注释(49-51「headingNumbering 关闭时不重置…全文档连续」)与代码矛盾**,佐证是 bug。【主会话抽查确认属实】
- [低] sniffImageType/mimeFromBuffer 兜底 "png"(image-type.ts:27,92):未知字节被打 png 标签,「用谎言兜底」。
- [低] UTF-16 BE BOM(FE FF)不识别(encoding.ts 仅 EF BB BF / FF FE)→ BE 落 gb18030 分支乱码;UTF-16 LE 分支 encoding 字段返回 "utf-8" 契约失真(当前消费方未爆雷)。
- [低] 白名单大小写不对称:开标签带 `i` 标志(pdf/render.ts:556)接受 `<STRONG>`,闭标签不带(550;html-whitelist.ts:32 同)→ 大写闭合标签整串转义;自闭合 `<br/>` 无处理分支(555-564 只认裸 `<br>`)。
- [低] merge.ts:27 IMAGE_RE 不感知代码块:围栏/行内代码里的示例图片语法也被改成绝对路径;index=0 文件 frontmatter 未剥离即参与替换。
- [低] 脚注重复引用产生重复脚注(render.ts:975-982 每次出现分配新 id)。
- [低] merge 分页符叠加(merge.ts:18,42 不检查尾部已有 page-break → 空白页;CSS 合并规则只覆盖 `.page-break + h1`,template.ts:44)。
- [低] eq label 识别口径差:docx collectPlainText 整段匹配(equations.ts:48)vs pdf 要求恰为单 text(pdf/render.ts:230-234),加粗/斜体包裹的 label 双格式结果不同。
- [低] JPEG SOF 扫描 128 段上限(image-type.ts:46)可接受。

## 维度 5:可维护性
- **[最重要] 运行时循环依赖 convert.ts ⇄ docx/render.ts ⇄ pdf/render.ts**:convert.ts:23-24 运行时 import 两 render;docx/render.ts:56 与 pdf/render.ts:16 反向运行时 import DEFAULT_PAGE_SETUP from "../convert.js"。【主会话抽查确认属实】settings-defaults.ts 本就是单一来源(convert.ts:29 已 re-export),两 render 各改一行 import 即解环。
- docx/render.ts(1085 行)仍是 god-module;pdf/render.ts 五个 override* 可按 rule 拆文件。
- 契约靠注释互指而非代码单源是最大系统性坏味道(见维度 1.2 四组)。
- INLINE_TAG_STYLES(inline-html.ts:46)与 ALLOWED_INLINE_TAGS(html-whitelist.ts:9)平行表:建议加 key 集合恒等测试断言。
- typography.ts:2-3 注释承认 renderer 侧平行 TypographySettings 定义,与 settings-defaults.ts「单一来源」宣称冲突——至少 type-only 共享。
- matchesPreset(settings-defaults.ts:175-193)逐字段手写比较:新增字段忘补则预设回填静默失准;建议字段数组驱动。
- i18n DICT 用 Record<string,string>:en 键集对齐无编译期保障,可用 `satisfies Record<keyof typeof zh, string>`;"preset.nameLimit"(214)全角逗号微小不一致。
- 注释密度高,「批次 N 功能 M」过程叙事偏多;高价值注释保持很好。

## 维度 6:安全
- HTML 注入主防线扎实:overrideHtmlRules 对 html_block/html_inline 双覆盖非白名单一律 escapeHtml;白名单禁一切属性;封面/标题/目录/title 均过 escapeHtml;锚点 id 白名单字符集。
- **[低-中] 用户 CSS 注入 `<style>` 未净化**:buildTemplate(template.ts:224)直接插值 `${css}`,pdfCss 与 loadKatexCss 产物均不过滤 `</style>` 序列——恶意 .css 可提前闭合 style 注入标记/脚本到打印窗口(该窗口无 CSP,见 main 报告 1.1/1.2)。防御一行:`css.replace(/<\/style/gi, "<\\/style")`。
- [低] mermaid SVG 原样内联(pdf/render.ts:671):本机隐藏窗口渲染用户自己的内容属自-XSS;建议 mermaid.ts 契约注释显式声明信任边界假设。
- 路径遍历:core 层几乎零 IO 符合架构承诺,merge.absolutizeImages 允许 ../.. 是相对引用正常语义,**无路径遍历风险**。

## 文件行数排行(core,Top 10)
| 排名 | 文件 | 行数 |
|---|---|---|
| 1 | docx/render.ts | **1085** |
| 2 | pdf/render.ts | **741** |
| 3 | i18n.ts | 565 |
| 4 | docx/math.ts | 274 |
| 5 | pdf/template.ts | 248 |
| 6 | pdf/bookmarks.ts | 236 |
| 7 | settings-defaults.ts | 194 |
| 8 | docx/inline-html.ts | 186 |
| 9 | comment.ts | 182 |
| 10 | docx/code-highlight.ts | 139 |

合计 ≈4440 行,前 2 名占 41%,恰是最需继续拆分的两个。

## core Top 10 改进点(影响×成本收益)
1. 解除 convert ⇄ render 循环依赖(docx/render.ts:56、pdf/render.ts:16 改从 settings-defaults.js 导入)——2 行解架构环。
2. 修复 headingNumbering=false 题注编号双格式分歧(captions.ts:60-66 与自身注释及 pdf 侧行为三方矛盾,先定契约再改一侧)。
3. frontmatter 误吞守卫(frontmatter.ts:24 至少命中一个已知 key 才认定)。
4. docx 图片 resolver memo 缓存(render.ts:1054 起 ctx 挂 Map)。
5. 四组人肉同步契约收敛代码单源(CROSS_REF_KINDS/sec-label 正则族/ImageResolver/白名单平行表)+一致性测试。
6. docx 悬空交叉引用警告去重(对齐 pdf unknownLabels 模式)。
7. renderList/renderBlockquote 不支持块类型降级或至少警告。
8. docxBookmarkId 截断加唯一化后缀(slug.ts:32,~5 行)。
9. pdfCss/KaTeX CSS 注入前净化 `</style>`(template.ts:208,224 一行)。
10. 拆分两大 render 文件(docx 抽 link-xref 与 cover/toc/header/footer;pdf 按 rule 拆 + 共用深度跟踪器)。

落选提名:GFM 表格对齐 docx 丢失、matchesPreset 字段驱动化、theme.ts 死常量清理、metadata date 失败不应兜底当前时间、转换警告文案纳入 i18n、loadKatexCss 失败上报警告通道。

---

# 二、src/main + src/renderer 审查报告(子代理 2)

## 维度 1:Electron 安全

做得好的:4 类窗口(主/预览/打印/mermaid)均 contextIsolation+sandbox+no nodeIntegration(index.ts:111-116,250;converter.ts:280;mermaid-service.ts:105-110);preload contextBridge 白名单;主窗口 CSP 严格(script-src 'self');webUtils.getPathForFile 适配;无 webSecurity:false/allowpopups。

问题:
| # | 位置 | 问题 | 建议 |
|---|------|------|------|
| 1.1 | core/pdf/template.ts:218-231 | **预览/打印 HTML 模板完全没有 CSP meta**;对比 mermaid 页面反而精心配了 CSP(mermaid-service.ts:46) | buildTemplate 加 CSP meta(default-src 'none'; img-src file: data:; style-src 'unsafe-inline' file:) |
| 1.2 | settings.ts:268 + template.ts:224 | 导入 pdfCss 原始字符串内联进 `<style>`;含 `</style><img onerror=…>` 可在无 CSP 的预览/打印窗口执行注入;100KB 上限只限大小不过滤内容 | 注入前剥离 `</style` 或校验不含标签结构 |
| 1.3 | index.ts:245-251 + pdf/render.ts:260-262 | **无 setWindowOpenHandler/will-navigate 拦截**(全仓库 grep 无命中);外链 href 进入预览 HTML 后点击即从 file:// 导航到外网站点 | 统一拦截导航 + shell.openExternal https 白名单 |
| 1.4 | index.ts:318,491-497,501 | IPC 参数类型校验缺口:convert 的 filePath/format、shell:reveal/open、preview:open 均未校验 typeof string;format 非 docx/pdf 静默落 pdf 分支(converter.ts:133);对比 settings/ui-state 有完善 sanitize,标准不一 | handler 入口统一类型守卫 |
| 1.5 | mermaid-service.ts:46 | CSP 含 unsafe-inline script-src 有实测注释(file:// 必需),隔离沙箱+securityLevel strict 下受控 | 记录已知接受项 |
| 1.6 | 全局 | 未配 session.setPermissionRequestHandler(默认拒绝,当前无权限需求) | 低优先 |

## 维度 2:主进程健壮性
| # | 位置 | 问题 | 建议 |
|---|------|------|------|
| 2.1 | index.ts 全文 | **无单实例锁**(grep requestSingleInstanceLock 无命中):双开各自持有 settingsCache/uiCache 与独立写队列,后写覆盖前写,用户感知「设置/最近文件丢失」 | requestSingleInstanceLock + second-instance 聚焦已有窗口 |
| 2.2 | index.ts 全文 | 无 process.on("unhandledRejection") 兜底;具体漏网:index.ts:119 void win.loadFile 无 catch;192 void win.loadURL(data:) 同理 | 入口注册全局兜底日志 |
| 2.3 | index.ts:128-137 vs 322/365/386 | **窗口关闭时转换进行中无处理**:close handler 直接保存 bounds 后 win.destroy(),不检查 ctxByWebContents 活动转换;销毁后 send 闭包抛 "Object has been destroyed";fs.writeFile 之后中断留半成品文件 | close 时有活动 ctx 则阻止关闭并弹确认(或先 cancel 等 finally) |
| 2.4 | smoke.ts:88,222-227 | **冒烟断言硬编码中文**(菜单「文件」「帮助」、守卫文案「请先选择 Markdown 文件」):语言 en 时 smoke 必失败 | 按 t() 字典取值或强制 zh 运行 |
| 2.5 | converter.ts:456 vs 261 | mergeConvertImpl 无条件 runAfterConvert,convertImpl 尊重 skipAfterConvert——同抽象层行为不一 | 对齐判断 |
| 2.6 | converter.ts:67 | resolverCache 模块级 Map 无淘汰,长会话单调增长 | LRU 或容量上限 |
| 2.7 | mermaid-service.ts:138-152,180-187 | withTimeout 超时后队列放行下一任务,上一 executeJavaScript 可能仍挂起,同窗口两次渲染交错窗口极小但存在 | 超时 destroy 重建窗口再放行 |
| 2.8 | index.ts:200-215 | refreshPreviewWindow 中 loadFile reject 时 oldCleanup 永不执行 → tmp 目录残留临时 HTML | catch 补 oldCleanup |
| 2.9 | index.ts:562-585 | activate 监听在 createWindow() 之后注册,macOS 极早期 dock 点击丢失 | 移到 createWindow 前 |

正面:atomic-json 原子写+写队列串行化、converter.ts:84-94 per-call ConvertContext 取消语义、mermaid-service.ts:115-117 渲染进程崩溃自动重建,设计扎实。

## 维度 3:renderer 质量
| # | 位置 | 问题 | 建议 |
|---|------|------|------|
| 3.1 | renderer.ts:192 | `const ERROR_MESSAGE = t("file.onlyMarkdown")` 模块加载期求值,setLanguage 尚未执行(loadSettings 异步),切英文后此错误文案仍中文 | 使用点内联 t() |
| 3.2 | state.ts:47-49 | converting 与 mode 双标志表达同一件事(mode!==null ⟺ converting),一致性靠各处同步维护 | 合并派生 getter |
| 3.3 | renderer.ts:659-662 | window unload 监听已废弃且页面常驻无需解除 | 删或 pagehide |
| 3.4 | dialogs.ts:42-52,113-115 | showCompleteDialog 未 hide 二次调用时旧 trapFocus 句柄被覆盖 → keydown 泄漏(当前流程互斥不会触发,防御性缺口) | 赋值前解除旧句柄 |
| 3.5 | mermaid-service.ts:67 | 唯一 innerHTML=svg:沙箱隐藏窗口+strict+SVG 来自 mermaid.render 自身输出,受控 | 记录已审查接受 |

正面:**零 XSS 面**——所有动态内容走 textContent/createElement/replaceChildren;事件监听模块级一次性绑定或委托,无逐渲染泄漏;trapFocus 严格配对。

## 维度 4:UX(可用性)
| # | 位置 | 问题 | 建议 |
|---|------|------|------|
| 4.1 | pure.ts:50 + converter.ts:289-300 | 单文件进度仅 3 档近似(read15%/render70%/done95%),大文档在渲染阶段钉死 70% 数分钟;printToPDF 原子调用不可中断(注释自认),取消在最耗时段形同虚设 | PDF 分阶段上报(parse/inline/katex/mermaid/print);print 阶段取消按钮置灰注明 |
| 4.2 | convert-flow.ts:54-60,63-65 | 错误提示直出底层异常原文(ENOENT 长路径等),无下一步指引;对比 preview.failed 文案带操作建议(i18n.ts:483),标准不一 | 常见错误码映射可操作文案(EBUSY→关闭占用后重试) |
| 4.3 | renderer.ts:427 | 转换中拖入文件被静默忽略,无反馈 | setStatus 提示「转换中暂不能添加」 |
| 4.4 | file-list.ts:216-221 + renderer.ts:220-228 | 重复添加的文件计入「跳过 N 个非 Markdown 项」语义误导;skipped 只报数量不报名单(collectMarkdownPaths 返回了路径数组却只取 length) | 重复项单独文案;skipped 列文件名(可折叠) |
| 4.5 | recent-files.ts:140-148 | 最近条目单击立即替换选择并开始转换,误触成本高(有「仅加载」次入口缓解) | 单击=加载、双击=重转,或 hover 区分 |
| 4.6 | index.ts:128-129 | 记忆 bounds 但不记忆最大化状态(有意为之有注释) | 可记忆 isMaximized |
| 4.7 | ui-state.ts:156 vs 61 | 注释说 panelOpen 缺省 true,实际 DEFAULT false——注释腐化 | 修注释 |

正面:设置收敛单一折叠面板 4 分组、字段级错误就地显示、焦点陷阱+Esc+焦点归还、Ctrl+Enter/Ctrl+O、sticky footer——键盘可达性超同类平均。

## 维度 5:i18n 残留
t() 干净(插值/回退/applyStaticTexts)。残留硬编码中文:
- converter.ts:140,146,231,421 warnings 文案(GBK 提示/输出目录回落)直达汇总条;
- converter.ts:224,411;index.ts:175 throw new Error 中文 → 失败状态栏/完成弹窗;
- core/pdf/render.ts:673,678;core/docx/render.ts:730 Mermaid 降级警告(core 层无 t());
- renderer.ts:701 title「Markdown 转换工具 v…」未走字典;
- smoke.ts:88,223 冻结中文(见 2.4);
- index.html lang="zh-CN" + 全部默认文案静态写死,en 用户启动到 loadSettings 完成前有中文 FOUC;
- renderer.ts:192 模块级 t() 冻结(见 3.1)。

## 维度 6:IPC 面
- channel 命名不一致:"convert" 是唯一裸名词(convert:batch/convert:merge 均 `域:动作`);
- 动宾序混乱:import:pdf-css vs presets:import;
- 数组元素未校验:paths:collect/filterExisting 只 guard Array.isArray,元素任意类型(path.resolve(non-string) TypeError);
- convert:progress 单通道靠 renderer mode 标志区分归属(renderer.ts:613-619 迟到事件依赖丢弃逻辑),能用但耦合。

## 维度 7:index.html/style.css
- **无暗色模式**:Win11 深色系统下纯浅色 UI,nativeTheme 全仓库未引用;
- 弹窗动画未尊重 prefers-reduced-motion(style.css:842,852,912-930);
- .settings-grid 固定 2 列无响应式断点(720px 最小窗宽勉强可用);
- 边距输入只有 min="0" 无 max(index.html:272-284),HTML 层与 JS 层钳制(MARGIN_MAX_MM)不对称;marginError 文案也只提 max(settings-logic.ts:400);
- 正面:role=status aria-live、progressbar 三 aria、dialog aria-modal/labelledby/describedby、:focus-visible 全覆盖、CSS 变量集中在 :root——可访问性基础扎实。

## main/renderer 文件行数排行(Top 10)
| 排名 | 文件 | 行数 |
|---|---|---|
| 1 | renderer/style.css | 1436 |
| 2 | renderer/renderer.ts | 702 |
| 3 | renderer/index.html | 651 |
| 4 | renderer/settings-panel.ts | 637 |
| 5 | main/index.ts | 589 |
| 6 | main/converter.ts | 519 |
| 7 | main/settings.ts | 370 |
| 8 | main/smoke.ts | 280 |
| 9 | renderer/utils.ts | 124 |
| 10 | renderer/state.ts | 80 |

结构评价:拆分纪律好——renderer.ts 组合根只做接线,逻辑分散 8 个子模块;main 分层清晰。下一个拆分候选:renderer.ts(702,事件绑定占半,可抽 events.ts)与 settings-panel.ts(637,绑定函数占 200 行)。

## 可用性影响 Top 5
1. **关闭窗口时转换静默中断无拦截提示**(index.ts:128-137)——大文档转换数分钟因误点 × 全作废,可能留打不开的半成品;唯一造成「数据损失感知」的问题。
2. **进度粒度过粗 + 最耗时阶段不可取消**(pure.ts:50 三档;converter.ts:298 printToPDF 原子)——大文档 70% 卡几分钟无法判断卡死还是在工作。
3. **无单实例锁**——双开静默数据互踩,「预设和最近文件莫名其妙丢了」极难自查归因。
4. **错误提示不可操作**(convert-flow.ts:54-60 直出原始异常)——预览链路已示范正确形态,转换链路应对齐。
5. **拖放与追加反馈缺陷组合**(4.3+4.4 三条叠加)——第一交互入口上反复试错。

总评:质量明显高于平均水平的代码库;短板集中在进程级健壮性收尾(单实例锁/关闭时转换处理/全局 rejection 兜底三个「最后 10%」)、预览窗口内容安全(模板无 CSP+CSS 注入面+外链导航未收口)、i18n 最后一公里残留。

---

# 三、测试体系与工程配置评估报告(子代理 3)

## 1. 自研零依赖测试框架

优点:
- acceptance.mjs(37 行)+ runner.js(50 行)极简自洽;段文件导出 async run() 自动发现,新增测试=新建文件,扩展成本趋近零。
- 逐段计时+全量汇总;失败不中断后续段,一次看全部失败面。
- 断言消息手写模板串普遍嵌入实际值(converter.test.js:136-137 全量上下文;basic-render.test.js:83 缺文本报 needle);runner 打印 error.stack。
- electron-mock(69 行)设计合理,头注释讲清 CJS 默认导出 SyntaxError 坑。
- fixtures 入仓/output gitignore 路径集中在 paths.js 单一来源。

问题:
1. **无逐段超时看门狗**(runner.js:40-48 只有 try/catch):任一段挂起整个进程挂死,CI 只能靠外部超时。
2. **并行执行目前不可行**:converter.test.js:229-272 与 mermaid-service.test.js:29-38 都临时替换 BrowserWindow.prototype.webContents getter;settings/i18n/ui-state 三段直接操作真实 %APPDATA% 用户文件(settings.test.js:39-50、ui-state.test.js:28),靠 backup/restore+query-string 动态 import 隔离——并发必互踩;console.log 拦截是全局副作用。10+ 纯 core 段理论上可 worker 化但需先解决 userData 隔离。
3. 断言失败无结构化 expect(actual).toBe(expected) 输出,作者忘拼 actual 时只剩一句话(basic-render.test.js:127)。
4. **docx 解包双轨制**:common/docx-utils.js:20 用系统 tar(依赖 PATH、每调用建删临时目录、同步阻塞),converter.test.js 用 jszip——同件事两套机制。
5. 耗时被少数 E2E 段主导(PDF printToPDF 真打印/mermaid 真渲染+硬编码 sleep/image-downloader http server 延迟),无总时长汇总输出。

## 2. 抽样测试段质量(6 段)
| 段 | 断言质量 | 脆弱性 | fixture 复用 |
|---|---|---|---|
| segments/basic-render.test.js(358 行) | 优:正负向兼备(:210 表头加粗+数据行不应加粗);坏语言注入测降级且 finally 清理(:320-334) | 高耦合精确 OOXML needle(CF222E/EMU/w:sz val="20" :130-134)——钉死版本下是有意契约断言,升级必碎 | 好 |
| segments/formula.test.js | 优:成功/降级/坏 katexDir/非∑回落四分支全覆盖,warnings 精确断言(:111-116) | 中:KaTeX MathML→OOXML 序列名升级敏感 | 好(双样例) |
| main/converter.test.js(347 行) | 优:取消三态+pdf 失败 finally 清理 tmp 断言(:261-262);toc:false 隔离分页符污染源(:195-197) | 中高:原型 patch/console 劫持/(2)后缀 regex shim(:99);SAMPLE_MD/PNG base64 内联未入 fixtures(:41-43) | 中(tmpdir 自建合理但未走 fixtures 体系) |
| main/gbk-encoding.test.js(53 行) | 优:iconv 写 GBK→warnings+中文往返,卫生完整 | 低 | backupSettings 复用 |
| segments/ipc-logic.test.js(108 行) | 优:纯函数边界全 | 低 | 无需 |
| main/mermaid-service.test.js(159 行) | 优:真实渲染+超时/畸形返回/崩溃(forcefullyCrashRenderer)/loadFile 失败/will-quit 兜底,崩溃自动重建断言 | 中:真实窗口+sleep,慢且环境敏感 | — |

总体:断言密度与边界覆盖显著高于同类平均;负面断言与清理恢复纪律高度一致。「序列化 needle 即契约」在依赖钉死前提下成立,是已文档化取舍而非疏忽。

## 3. tsconfig/eslint
- tsconfig.json:strict + noUnusedLocals/Parameters/noImplicitOverride/noFallthroughCasesInSwitch/forceConsistentCasingInFileNames,NodeNext+sourceMap(c8 映射闭环正确)。**缺 noUncheckedIndexedAccess**、noImplicitReturns、exactOptionalPropertyTypes。
- include 仅 src → test/scripts 不经 tsc 校验(JS 无 checkJs),类型安全只覆盖一半代码面。
- eslint.config.js:flat config correctness 导向(recommended + no-floating-promises/no-misused-promises/eqeqeq smart);projectService.allowDefaultProject 枚举 test/scripts 是官方方案。
- **tsconfig.eslint.json 是死文件**:全仓库仅一份 archive 审计文档提及,eslint.config.js 用 projectService 自动发现 tsconfig.json 从未引用——误导性死配置。

## 4. package.json scripts
- test = build && electron acceptance.mjs:build 耦合必要(56 处 ../../dist/ import,换零 ts-node 依赖),代价是每次全量 tsc 重编(未配 incremental/composite,无 .tsbuildinfo 缓存)。
- **test:smoke 不含 build**:裸 electron . --smoke 吃当前 dist,单独执行可能测陈旧产物而无任何警告(test:all 因先跑 test 间接保证新鲜,独立入口有隐患)。
- test:coverage:c8 --include dist/** + sourceMap 链路正确(实测 main 97%/core-docx 93%/core-pdf 95%/renderer 100%)。
- dist 配置干净:files 白名单+排除 hljs styles(主题 CSS 手写内联已确认安全)+electronLanguages 裁剪+publish never 由 CI 显式发布。

## 5. CI(.github/workflows/release.yml)
- **只有 tag 触发的 Release 流水线,没有 PR/push CI**(workflows 仅此一文件):typecheck/lint/test 只在打 tag 时跑,两次发版间 main 的破坏发布时才爆雷,违背「发布应是形式而非检验」。
- 好实践:windows-latest 平台一致;CI 官方 registry 覆盖本地镜像差异处理明确;CHANGELOG 提取 notes+generate-notes 回退;bash shell 显式声明避开 pwsh 坑。
- 缺失:无 concurrency 组(同 tag 重推并发冲突)、无 timeout-minutes(Electron 测试挂起烧满 6 小时上限)、失败无 artifact 上传。

## 6. scripts/
- copy-renderer.mjs(16 行)纯增量拷贝 html/css **无 clean**:src/renderer 删除/改名会在 dist/renderer 残留,tsc 同样不清 outDir,打包 dist/** 通配 → 陈旧资源可能进安装包。低概率但真实的脏产物风险。
- svg-to-ico.mjs(24 行)实现正确(sharp density=300 多尺寸+png-to-ico 合成)但未被任何 npm script 引用,建议补 icons script 入口登记。

## 7. 测试盲区清单(对照 src/ 53 文件)
| 盲区 | 说明 | 风险 |
|---|---|---|
| main/index.ts(589 行) | IPC handler 注册/runWithCtx 取消语义/预览窗口生命周期 mtime 刷新/菜单——**最大盲区**;纯逻辑已抽 ipc-logic.ts 缓解,runWithCtx 错误归一化/ctxByWebContents 注册释放/preview refresh-cleanup 零自动化 | 高(靠 smoke+GUI 实测) |
| renderer DOM 层整体 | renderer.ts/dom.ts/convert-flow/dialogs/file-list/recent-files/state/settings-panel 无 jsdom/happy-dom | 已知决策(R2 抽 pure.ts/settings-logic.ts 清零纯函数缺口),DOM 编排靠人工实测 |
| main/smoke.ts(276 行) | 仅 test:smoke 覆盖不在 acceptance 内 | CI(若有 PR CI)需决定是否纳入 |
| katex-dir.ts/mermaid-dir.ts(41 行) | dev/test/打包三态路径定位零测试 | 打包态路径错误只能 release 后暴露 |
| temp-html.ts/atomic-json.ts | 间接覆盖(tmp 残留断言/原子写断言) | atomic-json 作为共享基础设施值得直测段 |
| mdast-utils.ts/image-warning.ts | 经集成路径间接触达 | 微小可接受 |
| docx/theme.ts | 「集中字体配置」硬约束载体却无专门断言锁 eastAsia 字体规则(散落各段 needle) | 中 |

## 工程配置改进建议 Top 5
1. **补 PR/push CI**(最高优先):ci.yml windows-latest 跑 typecheck+lint+test(npm ci 复用 release.yml registry 覆盖模式),加 concurrency+timeout-minutes;半天成本把「发版才发现破坏」提前到每个 push。
2. **测试 userData 隔离**:acceptance.mjs 启动最早期 app.setPath("userData", <tmpdir>)——既是「测试污染开发者真实应用状态」的正确性问题,也是并行化前置条件。
3. **build 链路优化 + smoke 新鲜度守卫**:tsconfig 加 incremental:true;test:smoke 加构建产物校验(mtime 比对,过期报错或自动 build),堵「单独跑 smoke 测到陈旧 dist」。
4. **tsconfig 加严 + 清死配置**:启用 noUncheckedIndexedAccess;删除或接线 tsconfig.eslint.json。
5. **runner 加看门狗 + 汇报增强**:runSegment 包可配置超时;acceptance 末尾加总时长+最慢 5 段排行。顺手:统一 docx 解包机制(tar vs jszip 二选一)、copy-renderer 加 dist/renderer 清空。

总评:明显超出个人项目平均水准的测试体系——零注册发现、fixtures 单一来源(gen-fixtures 让自动化断言与 GUI 实测样例永不漂移,--check 幂等校验是亮点)、负面断言与状态恢复纪律严格、已知缺口有 G1-G9 审计闭环。核心短板不在测试本身而在门禁位置:质量关卡只在打 tag 生效,日常迭代缺自动化护栏;其次是真实用户目录依赖和全局原型 patch 限制隔离性与并行化。

---

# 四、主会话补充发现(文档层 + 抽查)

1. **convert.ts:12 头注释腐化**:「代码高亮:docx 等宽字体无高亮」——0.32.0 已加入 code-highlight.ts(docx 高亮),双管线差异注释未同步。
2. **docs/README.md:3 自述失实**:「Markdown 转 Word / PDF 等格式的命令行工具」——实际是 GUI 桌面应用(GUI 转向后未更新)。
3. 循环依赖论断抽查属实:docx/render.ts:56、pdf/render.ts:16 仍从 ../convert.js 导入 DEFAULT_PAGE_SETUP,而 settings-defaults.ts 已是单一来源(convert.ts:29 re-export 存在,main/settings.ts 也还在从 core/convert.js 导入)。
4. captions.ts 注释 vs 代码矛盾抽查属实(49-51 注释「不重置」vs 62-66 无条件重置)。
5. 文档体量:ACCEPTANCE 232 行 / CHANGELOG 364 行 / RESEARCH 171 行 / ROADMAP 85 行,均在维护约定线内;STATUS 56 行 ≤80 达标。USER-GUIDE 内容准确但 FAQ 偏薄(如「为什么我的公式没编号」「图片为什么不显示」等常见排障项缺失);安装节未提 Windows 版本要求/SmartScreen 未签名提示(ROADMAP 已砍签名,FAQ 应解释)。
6. README.md(项目根)功能特性列表 31 条偏营销向,与 USER-GUIDE 有少量重叠但口径一致;「npm run dev」前置 ELECTRON_MIRROR 环境变量只在 DEV-GUIDE 提及,根 README 安装节未提镜像要求(国内新环境 install 会卡)。
7. **WPS-COMPAT.md:37 目录条目失实**:「目录(静态标题列表 + 页码占位)」及备注「页码为转换时占位计算值」与实际实现矛盾——docx 目录无页码(render.ts:93「打开即见、可点击跳转、无页码」、render.ts:438 同),ACCEPTANCE 批次 8a 也断言「无页码」;系批次 8 D1 免更新路线拍板前的旧设计残留。且备注自相矛盾(「非域」却称「右键更新域可刷新」)。
8. **WPS-COMPAT 矩阵状态与结论脱节**:矩阵全部 ⬜ 待测,而 STATUS/ROADMAP/CHANGELOG 均记「Word/WPS 双实测通过(0.31.0)」;STATUS 有「矩阵状态列待用户回填或按通过处理」的挂起约定,至今未回填——守护性文档失去对照价值,建议回填或注明「按实测通过处理」。
