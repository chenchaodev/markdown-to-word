# 代码/测试/文档组织形式审计（批次 14+ 规划依据）

- 时间:2026-08-15 14:40:57
- 来源:@explorer exp-1(组织形式审计)+ @oracle ora-1(代码最佳实践与测试充足性审计)
- 关联:docs/RESEARCH.md 条目;验收 docs/ACCEPTANCE.md 批次 14+

## 一、组织形式审计（exp-1）

### 现状地图

- src/ 48 个 TS 文件约 10,900 行:core/(14 文件 ~800 行,纯净)+ core/docx/(6 文件 ~1,700 行)+ core/pdf/(5 文件 ~1,350 行)+ main/(11 文件 ~2,900 行)+ renderer/(13 文件 ~4,900 行)
- test/ 33 段 = segments 28 + main 5;fixtures/(acceptance 21 生成 md + manual/ 陈旧)+ common/(7 文件 239 行)+ tools/(gen-fixtures.mjs 242 行等)
- docs/ 9 文件 + archive/ 24 个存档(接近 >30 清理线)
- scripts/ 仅 2 个构建脚本;配置文件齐全(tsconfig 双文件/eslint flat/.npmrc)

### 可调整点清单（按收益/成本排序）

**高收益/低成本**
1. docs/README.md:49-55 同一 mermaid 条目重复 7 次(文档漂移实证,CHANGELOG 0.26.0 声称已去重但 README 登记表漏网)→ 删至 1 行
2. .gitignore 缺 coverage/(当前仅因通用 tmp/ 模式被意外忽略,侥幸)→ 显式加 coverage/
3. test/common/artifacts.js 头注释「output/test/artifacts/」vs 实际 output/artifacts → 改注释
4. docs/STATUS.md 开头 3 行悬挂在「# 状态速查」标题外 → 移入标题下

**中收益/低成本**
5. lint 只跑 src/(package.json:13),tsconfig.eslint.json 却含 test/scripts → test/scripts 实际未被 lint;建议 eslint src/ test/ scripts/(首跑可能修少量错误)
6. gen-fixtures.mjs 位于 test/tools/ 而非 scripts/ → 移入 scripts/ 或 DEV-GUIDE 注明(移动需同步 package.json 两处 script 路径)
7. test/fixtures/manual/ 陈旧遗留(RESEARCH 2026-08-13 已判定陈旧)→ 确认无人使用后删除或移 archive

**中收益/中成本**
8. settings.ts 与 ui-state.ts 双份「原子写 tmp+rename + 写队列」实现(~30-40 行重复)→ 抽共享工具(如 core/atomic-json.ts),测试已锁定
9. test/common/settings.js 与 src/main/smoke.ts 备份逻辑重复(注释声明有意:smoke 不得 import test/)→ 维持现状

**低收益/低成本**
10. preload.cts 与同目录 .ts 混用(.cts 为 CJS 输出所需,合理,文件头已有注释)
11. package.json build.files 排除 highlight.js/styles/** → 需确认 pdf 路线代码高亮是否依赖(若依赖则打包后高亮样式缺失)
12. docs/archive 24 个条目接近 >30 清理线 → 下次发版时顺手

**已确认合理、无需调整**:docx/render.ts(1015)与 style.css(1391)与 settings-panel.ts(561)单体大文件(RESEARCH 2026-08-11 已评估有意保留);renderer.ts(676)组合根已收敛;33 段零注册测试体系与 fixtures 生成器设计自洽;docs 职责划分清晰;.opencode/ 忽略、typescript 双别名、mermaid 钉死均为硬约束

## 二、代码最佳实践审计（ora-1）

### 重构候选清单

- **R1** theme.ts createDefaultStyles 死代码(33-43 行,0% funcs,全 src 无调用点)→ 删除(位于「集中字体配置」硬约束文件内,易误导)
- **R2** renderer DOM 层整体零覆盖(settings-panel.ts 553 行等全部 DOM 文件不在 c8 报告)→ 延续纯函数样板:状态计算/校验抽纯函数(settings-logic 模式),DOM 保持薄壳;近期不追求 DOM 层覆盖率
- **R3** settings.ts sanitize/校验函数未导出不可直测(非法边距回退 92-93 行等仅间接覆盖)→ 导出纯函数直测(presets-import 同模式)
- **R4** 回退策略并存:ui-state 字段级宽松回退 vs settings 整文件回退,无文档说明取舍 → 注释/docs 明确策略边界
- **R5** recent-files.ts ↔ convert-flow.ts ESM 循环依赖 → 共享状态抽独立模块或经 state.ts 单向流动(涉及 renderer 启动时序,需 GUI 实测回归)
- **R6** index.ts IPC 面 0% 覆盖(preload.cts/katex-dir.ts/smoke.ts 同 0%)→ handler 纯逻辑(参数校验/错误映射)抽可测模块,handler 保持薄壳;不做 Electron 集成测试
- **R7** docx/pdf 双管线契约漂移风险 → 勿合并(选型结论);渲染差异处加注释「与 docx 侧差异是有意的」,确保差异点有对应测试段
- **R8** 硬约束提醒:core 纯净性(settings-defaults 只依赖零外部导入纯模块)、依赖方向(converter→core 单向,反向禁止)、勿回退标注

### 测试缺口清单（按风险/收益排序）

- **G1** core/docx/math.ts 60.65% branch 全库最大缺口:munderoverToNary 非 ∑ 回落(252-264)、moText(267-274)→ 补 2-3 组
- **G2** core/pdf/postprocess.ts 75% stmts:embedExternalImages worker 错误/空结果与 URL 替换循环(90-116)、checkLocalImages catch(68-69)→ 注入失败断言降级
- **G3** core/pdf/bookmarks.ts:旧式 Dests 字典(82-84)、decodeURIComponent catch(100-101)、PDFDict 间接目标(109-115)→ 补 3 组
- **G4** core/pdf/metadata.ts 25% branch:无元数据原样返回(31-32)→ 补 1 组
- **G5** core/utils.ts:decodeNumeric 非法码点(20-21)、escapeRegExp(35-36)→ 补 2 组直测
- **G6** main/converter.ts:runAfterConvert open 失败(323-329)、pdf 分支(451-453)、collectMarkdownPaths stat 失败/非 md 直接路径(507-517)→ 补 3 组
- **G7** main/mermaid-service.ts:png 空(165-166)、catch(170)、退出兜底(200)→ 补 2 组
- **G8** 单分支小缺口 10 处(各 1 组):convert.ts:67 warnings ?? [] / merge.ts:39 空文件跳过 / captions.ts:87,113 chapter null、题注空文本 / equations.ts:52-53 孤立 label 警告 / docx render.ts:955-961 定义列表 blockquote/thematicBreak / pdf render.ts:109-110,660-663 hljs catch、mermaid resolver catch / template.ts:214-215 loadKatexCss catch / settings.ts:92-93 非法边距回退(配合 R3)/ ui-state.ts:167-168 非对象回退
- **G9** 已核实不补:encoding(GBK 链已被 gbk-encoding 覆盖)/ html-whitelist(矩阵循环充分)/ slug(纯函数小面)

### 优先级建议

1. 立即(低成本高收益):G5 + G8 + G4(~10 组断言,半天)
2. 近期(风险最高):G1(math.ts)+ G2(postprocess.ts)+ G3(bookmarks.ts)
3. 重构驱动(配合迭代):R1 删死代码 → R3/R4 导出 sanitize → R6 IPC 抽函数 → R2/R5 renderer 纯函数化
4. 不做:renderer DOM 层集成测试、index.ts Electron 集成测试(成本远超收益)

## 三、来源

- @explorer exp-1(组织形式审计,只读)
- @oracle ora-1(代码最佳实践与测试充足性审计,只读;覆盖率基于 coverage/tmp/ 既有 V8 dump 重生成,未重跑测试)