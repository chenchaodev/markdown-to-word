# 审计整改裁决与不做项记录（2026-08-24）

> 来源：《2026-08-24-134811-审计待办清单.md》61 项整改排期。
> 本文档记录本轮整改（P0~P5 全量执行）中经裁决**不做 / 暂缓 / 仅记录**的条目及理由，作为唯一否决依据；未列入本文档的条目均已实施。
> 裁决人：用户（DECIDE-1、ENG-3、执行范围三项经确认）；其余为编排器按业务价值判定。

## 用户裁决项

| 编号 | 裁决 | 说明 |
|---|---|---|
| DECIDE-1 | 统一为 Word 口径「1」 | 无 h1 文档的章节交叉引用，PDF 侧（xref.ts / CSS counter）改为跳过前导零级，与 docx prescan 一致；随 CORE-2 标题编号共享纯函数一并实现 |
| ENG-3 | 暂缓并记录 | 发布签名缺失需证书采购或签名方案选型（L 级外部依赖），待分发范围决策后另行立项。已知后果：用户首次运行遭 SmartScreen 拦截 |

## 不做 / 仅记录项

| 编号 | 条目 | 处置 | 理由 |
|---|---|---|---|
| CORE-12 | hasChildren 谓词提取（递归下降样板 ×3） | 不做 | 审计自评「非必需」；三处上下文各异，强行提取损害局部可读性，收益边际为负 |
| CORE-13 | content.ts 拆分 footnote.ts | 暂缓 | 审计自评「当前规模可接受」（212 行兼三场景）；设增长触发条件：再次新增块级场景时启动拆分 |
| TEST-15 | fixtures 图片双份字节拷贝 | 仅记录 | gen-fixtures 设计使然，acceptance 自包含收益换来的，属合理代价 |
| TEST-16 | smoke assertOutline 与 pdf-bookmarks 断言复制两份 | 仅记录 | smoke 不能 import test/ 的约束所致，去重需破坏隔离设计 |
| MR-18 | atomic-json 无 fsync、失败残留 tmp | 仅记录 | 桌面工具场景掉电风险极低，加固收益不抵复杂度 |
| DOC-10 | archive 命名规约漂移（24 份存档批量改名） | 改治理不改名 | 批量改名会断链既有文档互引；处置改为：自本批起新存档遵守规约，历史文件不加迁移注记（登记表本就不跟踪 archive 明细） |

## 执行中勘误（审计误判复核）

| 编号 | 原条目 | 复核结论 | 处置 |
|---|---|---|---|
| CORE-9(部分) | i18n-dict.ts 死键 preset.default/paper/business ×3 删除 | **审计误判**：三键经 src/renderer/index.html:207-209 以 data-i18n 属性运行期消费（applyStaticTexts 查表），删除会使 en 语言下模板预设下拉显示原始 key | 保留三键；「预设名走 i18n 还是删键」的二选一维持现状（硬编码中文 + i18n 键并存），后续如收敛须连同 index.html 同批处理 |

## 附：本轮范围裁决

- 执行范围：全部 P0~P5（约 54 项实施），车道划分见 STATUS.md 顶部登记。
- ENG-1（lockfile 版本同步）在审计落盘后已由提交 ac8a685 完成，本轮仅保留其剩余部分（发版 checklist + release.yml 校验，并入 ENG-2）。
