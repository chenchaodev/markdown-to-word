# 测试覆盖盘点(能力面 × 测试覆盖对照)

- 时间:2026-08-10 21:12:58
- 来源:@explorer(exp-1,两轮盘点:全量能力面 + 截断补全)
- 范围:src/core 全部模块、src/main(index.ts/settings.ts/image-downloader.ts)、src/renderer/renderer.ts
- 对照基准:test/segments/*.test.js(11 段)+ src/main/index.ts SMOKE 块(604-811 行)
- 用途:测试缺口清单依据(缺口清单见 docs/ROADMAP.md「测试缺口」节);换会话后如需了解某能力点覆盖状态,查本文件

覆盖标注:验收:段名 = test/segments 有断言段;smoke = SMOKE 块有断言;无 = 两处均无(间接覆盖会在说明中注明)。

## 一、src/core/convert.ts(格式注册表,纯逻辑)

| 能力点 | 职责 | 测试覆盖 |
|---|---|---|
| `convert(md, format, context)` | frontmatter 剥离 → 解析 → 缺失图片检查 → 分派 docx/pdf 渲染 | 验收:basic-render / formula / eq-numbering / footnotes / raw-html / typography / toc-caption / heading-links / pdf-meta / merge + smoke |
| `ConvertContext.baseDir` | 图片相对路径基准 | 验收(全部段)+ smoke |
| `imageResolver` | docx 图片读取回调 | 验收:basic-render(本地读取)、merge(smoke 亦测图片嵌入) |
| `title` | pdf `<title>` / docx 页眉 | 验收:footnotes / pdf-meta / formula 等;smoke(pdf) |
| `warnings` | 警告收集器 | 验收:eq-numbering(悬空引用警告);其余警告路径未断言 |
| `pageSetup` | 页面设置(缺省 DEFAULT_PAGE_SETUP) | 验收:merge(A4 纵向显式传值);smoke(landscape 端到端) |
| `typography` | 排版设置双格式共用 | 验收:typography / toc-caption(captionNumbering:false) |
| `breakBeforeH1` | 一级标题前分页 | **无**(smoke 只持久化该设置,未断言产物分页) |
| `toc` | 自动目录页开关 | 验收:toc-caption(toc:false 双格式) |
| `katexDir` | pdf KaTeX 资源目录 | 验收:formula / eq-numbering(@font-face 断言) |
| `collectMissingImageWarnings` | 缺失本地图片 → 警告文案 | **无**(toc-caption 用 missing-fig.png 未断言警告;fixture 10-附录.md 仅为输入) |
| `DEFAULT_PAGE_SETUP` / `PageSetup` | A4 纵向默认 25/25/32/32mm | 间接(默认路径全链路);边界未测 |

## 二、src/core/typography.ts

| 能力点 | 职责 | 测试覆盖 |
|---|---|---|
| `fontAscii` | 西文字体(默认 Calibri) | 验收:typography(仅随默认,渲染生效未单独断言) |
| `fontEastAsia` | 中文字体(默认微软雅黑) | 验收:typography(宋体参数化 + 断言) |
| `bodySizePt` | 正文字号 pt(默认 12) | 验收:typography(14pt → w:sz 28 / CSS 14pt) |
| `lineSpacing` | 行距倍数(默认 1.5) | 验收:typography(参数传入,间距值未直接断言) |
| `firstLineIndent` | 首行缩进 2 字符(默认 true) | 验收:typography(text-indent: 2em);docx firstLineChars 未断言 |
| `align` | left/justify(默认 justify) | 验收:typography(w:jc both + text-align) |
| `headingNumbering` | 章节编号(默认 true) | 验收:typography(false 关闭)、heading-links、pdf-meta |
| `captionNumbering` | 图/表题注编号(默认 true) | 验收:toc-caption(开 + 关闭) |
| `DEFAULT_TYPOGRAPHY` | 默认值快照 | 间接 |

## 三、src/main/settings.ts(AppSettings 持久化)

| 能力点 | 职责 | 测试覆盖 |
|---|---|---|
| 8 字段完整契约 | version/format/pageSetup/typography/breakBeforeH1/toc/afterConvert/outputDir | smoke(updateSettings/loadSettings 往返 breakBeforeH1 + landscape) |
| `SETTING_KEYS`(8 键) | patch 白名单 | **无**(sanitizePatch 白名单过滤未测) |
| `loadSettings` 惰性缓存 | 首读盘后缓存 | 间接(smoke 反复调用) |
| 文件损坏回退默认 | JSON 失败/形状非法 → 默认不写盘 | **无** |
| `saveSettings` 原子写 | tmp+rename 防半写 | **无** |
| `updateSettings` sanitizePatch | 非法值回退默认 | smoke(正常值);非法值/枚举外/类型错未测 |
| `sanitizePageSetup` 数值钳制 | 边距 0-1000mm 钳制 | **无**(边界 0/1000/负值未测) |
| `sanitizeTypography` 钳制 | 字号 8-24 / 行距 1.0-2.5 | **无** |
| 旧 settings.json 兼容 | toc/outputDir/typography 缺失兜底 | **无** |

## 四、核心小模块

| 模块/能力点 | 职责 | 测试覆盖 |
|---|---|---|
| `parse.ts parseMarkdown` | remark+gfm+math 解析,标题挂唯一 data.id | 验收:basic-render(直接调用);slug 去重逻辑未直接断言 |
| `frontmatter.ts parseFrontmatter` | 手写 YAML 子集(title/author/date),异常不报错 | 间接(footnotes/pdf-meta/merge 经 convert 断言);边界(引号剥离/注释/异常格式)未测 |
| `encoding.ts decodeMarkdown` | BOM 嗅探 + 严格 UTF-8 + GBK/GB18030 兜底 | 验收:encoding(四种输入全断言) |
| `slug.ts slugify` | 标题→slug 保留中文 | **无**直接单测(heading-links 中文书签名间接命中) |
| `slug.ts uniqueSlug` | 重复标题 -2/-3 | **无**(去重分支未测) |
| `slug.ts docxBookmarkId` | Word 书签限制兜底(数字前缀/40 字符截断) | **无** |

## 五、src/core/docx/render.ts 渲染特性

| 能力点 | 职责 | 测试覆盖 |
|---|---|---|
| 标题 1-6 级 renderHeading | heading 样式 + 书签 + 编号 + breakBeforeH1 | 验收:heading-links(h1-h3 书签/编号);**h4-h6 无**;breakBeforeH1 无 |
| 正文段落 renderBodyParagraph | 对齐/行距/首行缩进排版 | 验收:typography(w:jc both);行距/缩进值未断言 |
| 列表 numberingOptions | 有序/无序 + 0-3 级缩进 | **无**(basic-render/smoke 仅样例输入,未断言 w:numPr 列表) |
| 任务列表(docx 侧按普通列表) | remark-gfm taskList 节点 | **无** |
| 表格 renderTable | 100% 宽 + 全边框 + 表头加粗 | 验收:basic-render(`<w:tbl` 存在);表头 bold 未断言 |
| 代码块 renderCode | Consolas 10pt 逐行渲染 | **无**(有样例,无断言) |
| 引用块 renderBlockquote | 左缩进 + 灰底 F2F2F2 | **无** |
| 图片 imageToDocx | resolver → ImageRun 400×300,失败占位+外链警告 | 验收:merge(smoke:rels 含 image);尺寸/占位文本/警告未断言 |
| 行内样式(markdown 语法) | TextRun 样式传播 | 验收:basic-render(粗体文本);斜体/删除线/行内码序列化未断言 |
| 行内样式(白名单 HTML) | inlineHtmlItemsToRuns | 验收:raw-html(全序列化名 + 嵌套 + 危险样例跳过) |
| 脚注 renderFootnoteDefinition | 引用→收集器→FootnoteReferenceRun | 验收:footnotes(footnotes.xml 存在);重复引用 [^1] 双 id 未断言 |
| 公式(行内/display)texToDocxMath | MathML → Math,失败降级等宽灰字+警告 | 验收:formula(m:oMath/m:f/m:sSubSup/m:rad);**降级分支无** |
| 白名单 HTML normalizeInlineHtml | 合并/危险段丢弃/孤立闭标签丢弃 | 验收:raw-html(docx 侧跳过语义) |
| 分页符 `<!-- page-break -->` | 精确匹配 → PageBreak 段落 | **无**(smoke g3-smoke.md 有输入,无断言) |
| TOC renderTocPage | 静态目录免更新域 | 验收:toc-caption(TOC 指令/dirty=false/条目 anchor/toc:false) |
| 题注 renderCaptionParagraph | 图/表编号静态注入 | 验收:toc-caption(图 1.1/表 2.1/孤立行/开关关闭) |
| 封面页 renderCoverPage | title 居中加粗 22pt + author/date 灰字 | **无**(footnotes 等有 frontmatter title 输入,未断言 cover 段落) |
| 页眉 renderHeader | 标题居中 7pt 灰色 | 验收:footnotes(header1.xml 存在);内容未断言 |
| 页脚 renderFooter | 第 X 页/共 X 页 域 | 验收:footnotes(footer1.xml 存在);页码文案未断言 |
| 章节编号 headingNumberingOptions | h1-h3 挂 numbering(1/1.1/1.1.1) | 验收:heading-links(numbering.xml 模板)、typography(关闭无 numPr) |
| 书签 bookmarkChildren | BookmarkStart/End 唯一 linkId | 验收:heading-links(bookmarkStart 名称)、eq-numbering(eq-label 书签) |
| 公式编号制表位(center/right tab) | 居中公式+右对齐编号 | 验收:eq-numbering(w:val=center/right + (1)(2) 文本) |
| 内部锚点链接 | InternalHyperlink anchor | 验收:heading-links(w:anchor) |
| 外链/相对路径链接 | ExternalHyperlink / 假链接样式 | **无**(外链仅样例输入,序列化/rels 未断言) |
| 页面设置(纸张/边距/landscape) | pgSz + margin | 验收:merge(A4 显式);smoke(landscape w:orient);A3/A5/Letter/Legal/边距值无 |

## 六、src/core/pdf/render.ts 渲染特性

| 能力点 | 职责 | 测试覆盖 |
|---|---|---|
| 模板结构 buildTemplate | HTML 文档 + CSS 内联 | 间接(全部 pdf 断言段的载体) |
| 封面 buildCoverHtml | .cover 28pt 居中 + meta 行 | **无**(frontmatter 输入存在于 footnotes/pdf-meta/merge,未断言 .cover 结构) |
| 目录 buildTocHtml | 无页码锚点列表 + 分页 | 验收:toc-caption(class="toc" 存在 + toc:false 移除) |
| 脚注区 .footnotes CSS | 文档流末尾集中渲染 | 验收:footnotes(class="footnotes" + footnote-ref) |
| 公式 eq-block/eq-num | 编号静态注入 + 锚点 | 验收:eq-numbering(eq-block/eq-num/(1)(2)/id="eq:label"/悬空 (?) 文本) |
| 题注 fig/tab-caption | 前缀剥除 + CSS counter 伪元素编号 | 验收:toc-caption(class/前缀剥除/::before 规则/孤立行不标记) |
| CSS counter 章节编号 | h1c/h2c/h3c 伪元素 | 验收:pdf-meta(counter CSS 存在)、typography(关闭时无) |
| page-break → .page-break div | 分页符渲染 | **无**(pdf-meta 有输入样例,未断言 div) |
| 白名单 HTML(原样输出)/危险转义 | html_whitelist 组合 + escapeHtml | 验收:raw-html(原样/转义/嵌套/br) |
| KaTeX CSS 内联 loadKatexCss | 字体 file:// 绝对化 + 打印保护 | 验收:formula(@font-face 存在);file:// 改写/打印规则未断言 |
| 页脚模板 PDF_FOOTER_TEMPLATE | 第 X 页/共 X 页(9px 灰) | **无**(htmlToPdf 工具使用,页码文案未断言) |
| 任务列表 checkbox → ☐/☑ | replaceTaskCheckboxes | **无**(smoke 有 - [x] 样例,未断言替换字符) |
| 图片 file:// URL / 外链 data URL 内嵌 | overrideImageRule / embedExternalImages | **无**(本地图经 smoke 全链路,file:// 与 data URL 未断言) |
| 标题 id overrideHeadingIdRule | uniqueSlug 生成 + 去重 | 间接(经目录/书签断言) |
| 公式 label 行不渲染 + 未知 label 警告 | overrideEquationRule | 验收:eq-numbering(两格式) |

## 七、src/main/index.ts 行为点 + image-downloader.ts

| 能力点 | 职责 | 测试覆盖 |
|---|---|---|
| convertImpl 扩展名校验 | 仅 .md/.markdown | smoke(缺失文件失败项为 .md,间接) |
| convertImpl GBK 警告文案 | 「已按 GBK 编码读取」 | **无**(encoding.test 仅测纯函数层) |
| resolveOutputPath 重名保护 | 名 (2).ext 递增不覆盖 | smoke(仅兼容性处理剥离 (N),非主动断言) |
| resolveOutputPath 输出目录创建/回落 | mkdir 失败回落源目录 | **无** |
| resolveOutputPath 超长路径回落 | >250 字符回落 + 警告 | **无** |
| runAfterConvert(show-in-folder/open) | 导出后行为 | **无** |
| renderPdf 链路 | 临时 HTML → fonts.ready → printToPDF → 书签 → 元数据 → 落盘 | smoke(全链路:PDF 魔数 + Outlines 中文标题 + Dest[0] PDFRef);取消检查点无 |
| batchConvertImpl 并发池 | 并发 2、失败不中断 | smoke(3 成功 + 1 缺失,okCount/failCount/失败项/产物存在) |
| batchConvertImpl 取消 | canceledCount/未开始项标记 | **无** |
| batchConvertImpl 批量后 afterConvert | 仅第一个成功项 | **无** |
| mergeConvertImpl | 首文件 frontmatter 保留/后续剥离/图片绝对化/`-合并` 命名 | smoke(frontmatter + rels 图片 + 命名);merge.test.js(纯逻辑) |
| mergeConvertImpl 取消复位/进度 | cancelRequested 复位 + read/render/done | **无** |
| collectMarkdownPaths | 目录递归/点目录跳过/字典序/skipped | **无** |
| openPreviewWindow | 预览独立窗口 + 临时文件清理 | **无** |
| IPC dialog:openMarkdown / openMarkdowns / selectDir | 文件/多选/目录对话框 | **无** |
| IPC convert | 单文件转换(取消返回 canceled) | smoke(实现层直接调用;renderer 按钮点击仅 diag 不完成转换) |
| IPC convert:cancel | 置取消标志 | **无** |
| IPC convert:batch / convert:merge | 批量/合并入口 | smoke(实现层直接调用,非 IPC 通道) |
| IPC settings:get / settings:set | 读/更新持久化 | smoke(直接调用,非 IPC 通道;sanitize 边界无) |
| IPC shell:reveal / shell:open / preview:open | 文件夹/打开/预览 | **无** |
| image-downloader 本地读取 | path.resolve(baseDir, src) | 间接(basic-render/merge/smoke 经 resolver) |
| image-downloader http(s) 下载 | 10s 超时、仅 2xx、失败 null | **无** |
| image-downloader 同 URL 并发去重缓存 | 每文档实例缓存(含失败) | **无** |
| index.ts resolverCache 跨文件共享 | 按 baseDir 缓存 | **无** |

## 八、src/renderer/renderer.ts 功能点

smoke renderer diag(785-804 行)已断言:`window.api` 注入、convertBtn 存在 + 初始 disabled、点击按钮后 status 反馈、status--error 类、completeDialog 存在且启动隐藏。其余 GUI 交互均无自动化:

| 功能点 | 测试覆盖 |
|---|---|
| 文件选择对话框(openDialog/append 合并) | 无(仅按钮存在性在 diag) |
| 拖放(getPathForFile → collectMarkdowns → append 去重) | 无 |
| 单/多文件三态渲染 + 按钮显隐 | 无 |
| 多文件列表:上移/下移/移除/追加/清空/拖拽排序 | 无 |
| 格式选择 docx/pdf 持久化 | 无 |
| 设置面板全部控件(纸张/方向/四边距/H1 分页/TOC/导出后行为) | 无 |
| 排版面板(西文/中文字体/字号/行距/首行缩进/对齐/标题编号/题注编号) | 无 |
| 模板预设(TEMPLATE_PRESETS 套用/匹配回显/自定义提示) | 无 |
| 字段级错误提示(边距/字号/行距/字体空值回显) | 无 |
| 输出目录选择/重置 | 无 |
| 进度显示(阶段→百分比映射/进度条/aria) | 无 |
| 取消按钮(convertCancel + 禁用防重复) | 无 |
| 结果反馈:汇总条三态/警告折叠/失败详情重开 | 无(仅弹窗隐藏态在 diag) |
| 完成弹窗:复制路径/预览/打开文件夹/打开文件 | 无(diag 仅查启动隐藏) |
| 批量汇总弹窗逐条渲染/定位成功项 | 无 |
| 快捷键 Ctrl+Enter / Ctrl+O / Esc | 无 |

## 缺口速览(缺测试的高风险区)

封面页双格式、breakBeforeH1 产物、docx 代码块/引用/列表/任务列表序列化、外链图片下载(超时/去重/失败警告)、缺失图片警告文案、公式降级分支、取消链路(convert:cancel 全流程)、进度上报、resolveOutputPath 重名/回落、afterConvert、collectMarkdownPaths、预览、settings sanitize 边界、slug.ts 三个函数直接单测、PDF 页脚/checkbox 替换/file:// 图片、页面设置非 A4 纸张与边距、renderer 全部交互。
