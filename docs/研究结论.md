# 研究结论

### 2026-08-03 二期批次 1 实测事实(docx/pdf 排版控制,已验证)
- docx 9.x section `page.size`:orientation=landscape 时**库自动交换 width/height 写入 pgSz**,应传原始(纵向)尺寸 + orientation 枚举;手动交换会双重交换导致宽高反(实测 bug 已修复)
- docx 9.x 无 `IParagraphOptions.bookmarks`:标题书签用 `Bookmark` 组件包裹 runs(BookmarkStart/children/BookmarkEnd,产出标准 Word 书签)
- mdast `Data` 为空接口:标题 id 需 `declare module "mdast"` 声明合并(parse.ts),docx 消费端直接读 `node.data?.id`
- markdown-it 14.3 的 heading_open token 的 `content` 恒为空字符串,标题纯文本落在下一个 inline token(`tokens[idx+1].content`),标题 id 生成需该兜底
- 显式分页符 `<!-- page-break -->`(trim 精确匹配):docx 侧 mdast html 节点 → `PageBreak` run;pdf 侧 `html:true` + 白名单 override(其余裸 HTML 转义,与「raw HTML 白名单」同一机制)→ `.page-break` div(`break-before:page`;Chromium 打印实测可靠:两行字+分页符 → /Count=2)
- H1 前分页:docx 段落属性 `pageBreakBefore`;pdf CSS `h1{break-before:page}` + `body>h1:first-child{break-before:auto}`(防文档首元素空白首页)
- 设置持久化原子写:临时文件 + rename(Windows 下 rename 可覆盖);整文件形状校验失败整体回退默认
- 来源: 自查(迭代实测)+ fixer/designer 实现
- 关联: src/core/{slug,parse,convert}.ts、src/core/docx/render.ts、src/core/pdf/render.ts、src/main/settings.ts、里程碑 二期批次 1

### 2026-08-02 @oracle 评审二期开发计划结论(用户已确认采纳,已应用至路线图)
- 优先级调整:页面设置面板 **P1→P0**(贩卖中文排版护城河,PDF 侧 @page 参数化/docx 侧 Section,成本小);代码高亮主题 **P0→P2 或砍**(打印文档无需求);PDF 书签 **P1→P2**(全清单最硬:pdfjs 文本定位脆,先 spike 1-2 天,红线=H1/H2 最小版或砍)
- 砍:Mermaid(成本最高价值最低,无 DOM 需 hidden 窗口渲染)、CLI 转正(已有调试通道,价值低)
- 遗漏项:标题 slug/id 生成(**TOC/书签/内部链接公共底座**,mdast 标题现无 id,先行);拖放多文件/文件夹(批量转换输入前置);docx 导航窗格(render.ts 已用 HeadingLevel,Word 内置样式免费获得,零成本卖点)
- 依赖:设置持久化=底座(手写 userData JSON 即可,不引 electron-store,少依赖少打包风险;第一批做但不单独发版);分页控制+页面设置**必须同批**(共享页面上下文,拆批=渲染管线改两遍);PDF TOC 与 PDF 书签同一技术难点一次定型;docx TOC 与 PDF TOC 独立可并行
- 风险:docx TOC Word 域=中(半天 spike,docx 9.x SimpleField/ComplexField 可拼 TOC \o "1-3";**验收 Word+WPS 双实测 F9**;红线=静态标题列表+F9 提示+免费导航窗格);PDF 书签=高(标题→页码映射是难点,Chromium 拆 span 匹配脆);分页符=低(pageBreakBefore/PageBreak run,半小时);外链图片下载=中低纯工程(超时 10s/并发 3/去重;**失败策略=保留原 URL+警告**,与缺失图片警告同构,保真优先);KaTeX=小-中(字体需 data URL 内嵌/拷贝,一周内);OMML=高(只 spike 不承诺交付,预设子集+降级纯文本兜底)
- 建议批次:1 排版控制+设置底座(设置持久化/导出后行为/分页控制/页面设置/标题 slug) / 2 保真+正式文档化(外链图片/TOC/封面+YAML/PDF 预览) / 3 批量+长文档(批量转换/PDF 书签 spike/多文件合并/脚注)
- 必决取舍:①PDF 目录=无页码+锚点先行,**先实测 printToPDF 是否保留页内锚点为 PDF 链接**;②docx TOC 域开工即 spike;③显式分页符语法建议 `<!-- page-break -->`(不占 --- 的 hr 语义,兼容 Typora 生态);④外链下载失败=保留原 URL+警告
- 理由:@oracle 评审(已读路线图/研究结论/ADR/状态速查 + 抽查 convert/render 实现)
- 来源: @oracle(ora-1)
- 关联: docs/路线图与迭代规划.md 二期规划

### 2026-08-02 竞品功能矩阵与用户需求调研(md→Word/PDF 同类工具)
- 结论(完整矩阵见路线图「二期规划」与调研原文):
  - **中文排版是行业顽疾,也是我们护城河**:Pandoc 修 eastAsia 字体问题修了 4 年(issue #7022 → PR #10901,2025 才默认 zh-CN);markdown-pdf 中/日/韩字体 issue 长期未决;我们 G1 已实测 eastAsia 正确写入
  - **图片丢失/路径问题是行业 #1 转换投诉**(markdowntoword.pro 故障指南 15 个高频问题图片占 3 席);我们已有缺失图片警告,补「外链下载 + 转绝对路径」即闭环
  - **批量转换呼声极高**(MarkText #2739、Obsidian Better Export PDF 核心卖点);PDF 直出有真实需求(Markdown All in One #270 多年未实现)
  - **PDF 书签大纲**:printToPDF 不产出书签,需后处理注入(OBSIDIAN Better Export PDF 正因补书签而存在);是差异化点但属半硬骨头
  - **真 docx + 本地离线组合是硬卖点**:用户已学会用「导航窗格有无标题」判别 HTML-rename 假 docx;在线工具「上传即泄露」是普遍顾虑
  - TOC:docx 用 Word 域(可 F9 更新),PDF 用静态目录;竞品几乎全有
  - 外链图片在线工具普遍下载嵌入;raw HTML 转换安全风险高(默认禁用/白名单)
  - 转换保真:在线工具部分为 HTML-rename 假 docx(虚假 Word 文件)
- 理由:@librarian 调研(来源:Pandoc Manual/Typora Export/Markdown PDF README/Better Export PDF/GitHub issues 等)
- 来源: @librarian(lib-1)
- 关联: docs/路线图与迭代规划.md 二期规划

### 2026-08-02 G5 打包坑:highlight.js es/ 不可排除(已验证,勿回退)
- 结论:`import hljs from 'highlight.js/lib/common'` 在 ESM 下经 exports map 的 **import 条件解析到 `./es/common.js`**,打包时若用 `!node_modules/highlight.js/es/**` 排除该目录,asar 内模块解析失败 → 主进程启动即 `ERR_MODULE_NOT_FOUND`(静态 import 链:main → convert → pdf/render → highlight.js,启动即加载)
- 修复:files 中**移除 es/ 排除**(体积代价 ~2-3MB asar/~0.3MB 安装包,可接受);styles/ 可继续排除(模板自带 .hljs 颜色,无需主题 CSS)
- 教训:排除 node_modules 子目录前必须核对该包 exports map 的 import/require 条件目标;**dev/smoke 全绿 ≠ 打包可用**(dev 走完整 node_modules)
- 来源: 自查(用户报错截图 @observer 定位 + 排查)
- 关联: package.json files 配置,里程碑 G5

### 2026-08-02 G5 实测事实(electron-builder 打包,已验证)
- 结论:
  - electron-builder 26.15.3 + `"type":"module"` + `"main":"dist/main/index.js"` 实测打包成功,ESM 入口无需任何特殊配置(asar 默认开,纯 JS 无需 asarUnpack)
  - `files: ["dist/**","package.json","!node_modules/highlight.js/es/**","!node_modules/highlight.js/styles/**"]` 实测:asar 内 dist 完整、highlight.js es/styles 排除(0 条)、lib/languages 保留(385 条,lib/common 依赖)
  - NSIS 安装包 `MarkdownToWord-Setup-0.5.1.exe` 88.9MB;静默安装 `/S /D=路径` 退出码 0、安装版启动 OK;静默卸载 `/S` 退出码 0、目录清除
  - `ELECTRON_BUILDER_BINARIES_MIRROR=https://npmmirror.com/mirrors/electron-builder-binaries/` 实测下载成功(nsis-3.0.4.1/nsis-resources-3.4.1 100%)
  - 产物目录被系统进程锁(EBUSY unlink app.asar):electron-builder 重建 win-unpacked 失败;绕道改 `directories.output` 打包;锁来自 Defender/索引类系统服务(非本应用进程),资源管理器/索引空闲后或重启释放
  - CLI 覆盖参数 `-c.directories.output=x` 会被解析为配置文件路径报 ENOENT,改输出目录应直接改 package.json
  - 打包版 `--smoke` 不可用:smoke 写 output/ 位于 asar 内(只读);打包版验证用「启动存活 + asar list + 静默安装/卸载」
- 理由:全部经本项目实际打包/安装/卸载验证
- 来源: 自查 + @librarian(lib-2 调研)
- 关联: 里程碑 G5,package.json build 配置

### 2026-08-02 G5 调研:electron-builder 打包方案(带来源,待实测)
- 结论:
  - **electron-builder 26.15.x**(最新 26.15.7;27 仍是 alpha 勿用);ESM 主进程自 v24.11 动态 import 支持、v24.12 修复 ERR_REQUIRE_ESM,`"type":"module"` + `"main":"dist/main/index.js"` 可直接打包,无需特殊配置
  - **`directories.output` 必须设 `release/`**:默认 `dist/` 会与 tsc 产物混目录(builder-effective-config.yaml 等污染)
  - `files: ["dist/**", "package.json"]`;production node_modules 自动包含,勿手动写;纯 JS 依赖无原生模块 → **不需要 asarUnpack**(asar 默认开)
  - NSIS:捆绑 29 语言含 zh_CN(中文系统自动简体);`oneClick: false` + `allowToChangeInstallationDirectory: true`(向导式,默认按用户安装不弹 UAC);捆绑 NSIS 3.0.4.1 有 CVE-2025-43715(已知,跟踪 issue #9141)
  - 镜像正确地址:`ELECTRON_BUILDER_BINARIES_MIRROR=https://npmmirror.com/mirrors/electron-builder-binaries/`(**带 /mirrors/**,registry.npmmirror.com/electron-builder-binaries/ 404,已 HEAD 实测);electron-builder 写死工具链版本(nsis-3.0.4.1/winCodeSign-2.6.0),镜像同步延迟会 404,首次成功缓存于 %LOCALAPPDATA%\electron-builder\Cache
  - 验证:`npx electron-builder --win nsis --x64 --publish never`;快速冒烟 `--dir`;asar 内容 `npx @electron/asar list release/win-unpacked/resources/app.asar`;免安装运行 `release/win-unpacked/<productName>.exe`;NSIS 静默装 `Setup.exe /S /D=路径`
  - 体积:Electron 43 本身 ~180MB(不可削减);`win.electronLanguages: ["zh-CN","en-US"]` 裁 locales 收益最大;highlight.js 无 tree-shaking,排除 `!node_modules/highlight.js/es/**` 与 `!node_modules/highlight.js/styles/**`(lib/common 静态 require 35 种语言,lib/languages 不可整体排除)
- 理由:@librarian 调研,官方文档/源码/registry 实测 + 镜像 HEAD 实测
- 来源: @librarian(lib-2)
- 关联: 里程碑 G5,package.json build 配置

### 2026-08-02 G4 实测事实(printToPDF 管线,已验证)
- 结论:
  - `@mdit/plugin-tasklist@1.0.2` peer 要求 markdown-it ^14.2.0,与 15.0.0(2026-07-30 发布)冲突 → 项目固定 markdown-it **14.3.0**(同样原生 ESM),插件生态按 14 系演进
  - `printToPDF({ pageSize:'A4', margins:0, printBackground:true, preferCSSPageSize:true, displayHeaderFooter:true, footerTemplate })` 在 Electron 43 实测成功;margins 设 0、边距交给 `@page { size:A4; margin:18mm 16mm 22mm }`(preferCSSPageSize 生效)
  - 任务列表 checkbox 打印 bug 规避方案实测有效:渲染后把 `<input class="task-list-item-checkbox" type="checkbox" disabled[ checked]>` 替换为 ☐/☑ 字符 + `li.task-list-item { list-style:none }`,PDF 输出为 Unicode 符号(observer 实测截图确认,非原生控件)
  - 图片统一转 `pathToFileURL` 绝对路径(override image rule)实测可用:相对路径图片正确嵌入 PDF
  - `footerTemplate` 必须内联样式(9px 灰字),页码「第 X 页 / 共 X 页」正常
  - 代码高亮:`import hljs from 'highlight.js/lib/common'` + highlight 回调 + `ignoreIllegals:true` 实测可用;PDF 需 printBackground 才有底色(已开)
  - smoke 已扩展 pdf 链路(中文/表格/代码块/任务列表/删除线/图片/页码,魔数 %PDF- 校验)
- 理由:全部经本项目实际运行验证(smoke + PDF 产物 + observer 视觉确认)
- 来源: 自查 + @librarian(lib-1 调研)+ @observer(obs-1 视觉验收)
- 关联: src/core/pdf/render.ts,src/core/convert.ts,里程碑 G4

### 2026-08-02 G4 调研:markdown-it → HTML → printToPDF 管线(带来源,待 G4 实测)
- 结论:
  - markdown-it 15.x 原生 ESM(内置 TS 声明,无需 @types);**表格/删除线核心内置默认开启,零插件**;任务列表唯一需插件:`@mdit/plugin-tasklist`(mdit-plugins 官方 TS 集合,活跃维护);`markdown-it-gfm` 包不存在(npm 404)
  - highlight.js:`import hljs from 'highlight.js/lib/common'`(ESM exports 映射 `./es/common.js`),highlight 回调 + `ignoreIllegals: true`;CSS 主题经模板 `<link>` 引入;**printToPDF 必须 `printBackground: true` 才带代码块底色**
  - printToPDF 固定参数:`pageSize: 'A4'`(默认 Letter!)+ `printBackground: true` + `preferCSSPageSize: true`(否则 `@page` size/margin 被忽略,issue #38674);@page 存在时 `landscape` 选项失效(issue #8365),方向写 CSS;页码走 `footerTemplate` 的 `pageNumber`/`totalPages` 类,`@page` margin box 页码不可靠
  - 图片:HTML **落盘临时文件 + `webContents.loadFile()`**,file:// 下相对 src 自动解析;data URL 加载才需要 `baseURLForDataURL`
  - 分页:`break-before: page`(别名 `page-break-before: always`)在 Chromium 打印可靠;`break-inside: avoid` 有兼容缺口,强制分页最稳
  - **已知坑(待实测)**:Chromium 打印原生 checkbox 有 bug(勾选态不显示/全黑,issues 40627907/40350661)→ 任务列表用 CSS `appearance: none` 自定义外观
- 理由:@librarian 调研,官方文档/Chromium issue/2026 实测来源
- 来源: @librarian(lib-1)
- 关联: 里程碑 G4,src/core/pdf/render.ts

### 2026-08-02 md→pdf 路线修订:弃 md-to-pdf,改 Electron printToPDF
- 结论:pdf 路线由 `md-to-pdf`(5.2.5)改为「markdown-it → HTML 模板 → `webContents.printToPDF()`」(Electron 43 内置,与 puppeteer `page.pdf()` 同源)
- 理由:Electron 自带 Chromium,保留 md-to-pdf 会重复下载 ~300MB 第二个 Chromium,且其 9 个月无更新;自研 HTML 模板将来可复用做预览
- 来源: @librarian + @oracle
- 关联: docs/架构决策.md ADR-002
- 取代:下方「md→pdf 库调研」条目中 pdf 部分

### 2026-08-02 Windows GUI 方案调研
- 结论:选 **Electron**(43.2.0,2026-07);Tauri 体积小但 Node 代码需 sidecar 改造;本地服务+浏览器体验差;neutralino 社区小
- 理由:主进程即 Node,现有 TS 管线直接 import;一份 Chromium 两用(GUI + PDF 打印)
- 镜像(勿回退):安装设 `ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/`;electron-builder 另需 `ELECTRON_BUILDER_BINARIES_MIRROR`(winCodeSign/nsis)
- 来源: @librarian
- 关联: docs/架构决策.md ADR-002

### 2026-08-02 md→docx / md→pdf 库调研(规划选型用)
- 结论:docx 路线选 `docx`(9.7.1,2026-05-27)+ remark 自研渲染;**pdf 部分已被 2026-08-02「md→pdf 路线修订」条目取代**
- 理由:
  - docx:中文 eastAsia 一等支持(`font: { ascii, eastAsia, hAnsi }`),原生 ESM+TS,600 万下载/周,维护稳定;`@mohtasham/md-to-docx`(3.0.1,2026-07-31)仅暴露 fontFamily 无独立 eastAsia,留作回退;`markdown-docx`(vace)无字体入口,排除
  - pdf(原结论):md-to-pdf 一行 API 内置 markdown-it+语法高亮+分页;中文靠系统字体(Linux 缺 noto-cjk 会出方块)或 CSS `@font-face` 内嵌;`pdfmake` 需手动注册 TTF 字体,最不省事
- 来源: @librarian
- 关联: docs/路线图与迭代规划.md

### 2026-08-02 G1 实测事实(docx 9.x + remark 管线,已验证)
- 结论:
  - `docx` 9.x 的 `Document` 直接收 `numbering: INumberingOptions` 对象,不需要 `new Numbering()` 实例
  - `TextRun` 无公开可变 `options` 字段,行内样式用构造参数累积传递(不可事后改)
  - mdast 中 `image` 是 **PhrasingContent 行内节点**(嵌在 paragraph 内),不是块级节点;行内渲染需支持 `ImageRun`
  - `ImageRun` 的 type 枚举是 `png/jpg/gif`,魔数判断要返回 `"jpg"` 而非 `"jpeg"`
  - Windows 下生成 docx:设置 `font: { ascii, eastAsia, hAnsi }` 后 document.xml 正确写入 eastAsia,中文字体生效
- 理由:全部经本项目实际运行验证(typecheck + 生成 docx + 解包检查 XML)
- 来源: 自查
- 关联: src/core/docx/render.ts,里程碑 G1

### 2026-08-02 待实测事实(G1/G4 验证后更新本条)
- 结论:~~docx 传字符串字体名会自动写全 ascii/hAnsi/eastAsia/cs 四属性~~(G1 已实测:传对象字体四属性全写,字符串字体名未测);Electron `webContents.printToPDF()` 在 ESM 主进程下按官方 API 可用(待 G4 实测)
- 理由:来自文档/社区结论,printToPDF 部分未在本项目实际运行验证
- 来源: @librarian + @oracle
- 关联: 里程碑 G4
